# S4_cog_9dot0_nested_orbit.md

## Formula candidate

```text
S₄(m1, m2, m | U)
=
{
  COG = center(m1, m2, m),
  dot = central_equilibrium_reference_point,
  0 = COG field,
  9 = orbit endpoint of m2,
  sequence = dot → 0 → 1...8 → 9,
  next = repeat_9dot0(m2, m),
  dimension = n + k or n - k by boundary depth
}
```

## Role

S₄는 COG–9dot0 중첩궤도와 effective dimension rank를 다룬다.

## Status

중요하지만 아직 완전 검증은 보류되었다.

```text
S₄ = hold / not discarded
```
