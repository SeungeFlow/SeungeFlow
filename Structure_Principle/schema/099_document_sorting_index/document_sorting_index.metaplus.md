# METAPLUS

id_reference: schema.099.document_sorting_index  
schema_name: document_sorting_index  
source_file: document_sorting_index.meta.md  
metaplus_file: document_sorting_index.metaplus.md

purpose:
- 이 문서는 원본 document_sorting_index.meta.md를 대체하지 않는다.
- 이 문서는 schema.099.document_sorting_index에 대해 사용자와 인공지능이 나눈 대화내용을 정리한다.
- 이 문서는 구조원리를 새로 만들거나 relation을 새로 확정하기 위한 문서가 아니다.
- 이 문서는 대화정리형 이해 로그다.
- 이 문서의 핵심 목적은 승이의 실제 입력글, source_meta, AI 인스턴스 대화층을 섞지 않고 분리하는 것이다.
- 이 문서는 099_document_sorting_index가 vector_unlock처럼 강하게 열린 후보 흐름을 전부 meta.md로 만들지 않고, 실제 062~099 생성 흐름을 원리 객체 / 해석층 / 예시 / reference_only / pending으로 분류하는 sorting gate임을 보존한다. :contentReference[oaicite:0]{index=0}

conversation_context:
- 이 문서는 ChatGPT.making 대화에서 생성된 이해정리본이다.
- 원본 document_sorting_index.meta.md 수정본이 아니라 대화정리층이다.
- 이번에 붙여진 내용은 ChatGPT.direct 형식의 AI 인스턴스 대화층과 승이의 직접 입력이 함께 제공되었다.
- 동시 업로드된 자료에는 document_sorting_index.meta.md, vector_unlock.meta, understanding_flow_001~004 흐름이 포함되어 있다.
- 따라서 이 문서는 099 자체만이 아니라 vector_unlock과 understanding_flow가 왜 함께 주어졌는지를 보존한다.
- document_sorting_index.meta.md 원문 내용은 source_meta로 보고 keep_as_original에 보존한다.
- vector_unlock은 061 이후 후보 가능장으로 분리해 이해한다.
- understanding_flow는 form / forming / formed / Coremap 관계를 보강하는 대화층으로 분리해 이해한다.
- AI 해석을 사용자 입력처럼 쓰지 않는다.
- 사용자 입력과 AI 인스턴스 대화층을 섞지 않는다.

## 1. user_input_summary

[승이의 입력글]

승이는 다음 취지로 두 파일을 함께 제공했다.

```text
document_sorting_index.meta
vector_unlock.meta

파일 2개를 주는 이유는 내용을 보면 안다.
```

이 입력의 의미는 다음으로 이해된다.

```text
vector_unlock =
061 이후 후보 가능장

document_sorting_index =
후보 가능장을 전부 meta.md로 만들지 않고,
무엇을 schema로 만들고,
무엇을 해석층 / 예시 / reference_only / pending으로 둘지 가르는 sorting gate
```

또 승이는 understanding_flow 자료를 함께 제공했다.

여기서 핵심은 다음이다.

```text
Schema =
form

Schema relation 작업 =
forming

Snapshot =
formed

Coremap =
formed 사이 relation map
+
formed를 다시 forming으로 여는 gate

현재 작업 상태 =
forming
```

승이는 또한 인간계 비유를 통해 다음을 설명했다.

```text
계 이름 =
visible label =
meta.md 파일명

계 내부 흐름 =
hidden scalar =
core / shortest / 압축 form

Coremap =
hidden scalar relation map
```

요약:
- 승이는 099를 단순 문서정리 문서가 아니라, 후보 가능장과 실제 schema 생성 흐름을 분리하는 sorting gate로 보라고 했다.
- 승이는 vector_unlock과 099를 함께 보아야 061 이후 후보 흐름과 실제 062~099 생성 흐름의 차이를 알 수 있다고 보았다.
- 승이는 100_understanding_flow가 reserved seat이며, 099 다음은 101이라는 점을 문서번호가 아니라 visible coordinate로 읽어야 한다고 보았다.
- 승이는 schema를 form, relation 작업을 forming, Snapshot을 formed, Coremap을 formed→forming gate로 이해했다.
- 승이는 meta.md 파일명과 내부 core / shortest의 관계를 인간계의 visible system name과 hidden scalar 관계로 비유했다.

## 2. ai_response_summary

[AI 인스턴스 대화층]

- AI 인스턴스는 schema.099.document_sorting_index / document_sorting_index.meta.md를 분석 대상으로 수신했다.
- 동시에 vector_unlock.meta를 함께 읽었다.
- AI 인스턴스는 파일 2개를 같이 준 이유가 보인다고 판단했다.
- 099_document_sorting_index는 문서정리 index이고, vector_unlock은 061 이후 후보 리스트이다.
- 두 문서를 함께 보면 099가 왜 필요한지가 선명해진다.
- vector_unlock은 061 이후 가능한 schema 후보들이 매우 강하게 열린 상태였다.
- 하지만 그 후보들을 전부 active meta.md로 바로 만들면 구조가 꼬인다.
- 그래서 099가 필요하다.
- AI 인스턴스는 099를 다음처럼 이해했다.

```text
대화내용 전부 문서화 X

원리 객체만 meta.md

나머지는
relation / example / source_trace / pending
```

- AI 인스턴스는 099를 vector_unlock처럼 강한 후보 리스트가 발생했을 때, 무엇을 실제 schema로 만들고, 무엇을 interpretation / example / reference_only / pending으로 둘지 가르는 sorting gate로 읽었다.
- AI 인스턴스는 099가 098_reference_only_high_density_trace_index 이후에 자연스럽게 드러난다고 판단했다.

## 3. source_meta_understanding

[SOURCE_META]

099_document_sorting_index의 구조 이해는 다음으로 정리된다.

```text
document_sorting_index =
document sorting index
schema creation sorting gate
principle entity / interpretation / applied example / reference_only / pending classification index
anti-overdocumentation rule
100_understanding_flow reservation guard
```

### core

```text
099 =
문서정리 index

100 =
reserved

099 → 101

원리 객체만 meta.md
```

### definition

```text
document_sorting_index는
지금까지 대화에서 풀린 고밀도 개념들을
meta.md로 대량 생성하지 않고,
원리 객체 / 관점 해석 / 적용 예시 / reference_only / pending으로 분류하기 위한
일회성 정리 index schema이다.
```

### sorting_classes

#### A. principle_entity

```text
principle_entity =
원리 객체

판정 =
meta.md 후보
```

#### B. interpretation_layer

```text
interpretation_layer =
관점 해석

판정 =
기존 meta.md의 note / interpretation / relation_reason으로 들어감
```

#### C. applied_example

```text
applied_example =
적용 예시

판정 =
example / renderer_hint / applied_case / source_trace로 들어감
```

#### D. reference_only

```text
reference_only =
강하지만 본류에 바로 넣으면 위험한 고밀도 trace

판정 =
reference_only index에 보존
```

#### E. pending

```text
pending =
방향은 있으나 이름 / 위치 / relation이 확정되지 않은 항목

판정 =
pending registry에 둠
```

### numbering_rule

```text
061_vector_unlock 이후
새 schema는 062부터 시작했다.

100은 이미 reserved:
100_understanding_flow

따라서:
099 다음은 101
```

### important_sentence

```text
숫자는 문서번호가 아니라
directory seat / visible coordinate이다.
```

### forbidden

```text
대화에서 나온 모든 내용을 meta.md로 만들지 않는다.

비유를 독립 원리로 과잉 승격하지 않는다.

과학 구현 예시를 과학 검증으로 오해하지 않는다.

reference_only를 active_schema로 바로 승격하지 않는다.

원리 객체와 관점 해석을 섞지 않는다.

100_understanding_flow 번호를 침범하지 않는다.

번호를 단순 문서순서로만 보지 않는다.
```

## 4. vector_unlock_relation

vector_unlock은 061 이후 열릴 수 있었던 후보 가능장으로 이해된다.

vector_unlock은 060_coherency 이후 흐름을 다음처럼 열었다.

```text
coherency
→ dot_dot_dot_system
→ vectorizing
→ vector_rize_ride
→ objectifying
→ shape_before_meaning
→ vectorized_cell_4pin
→ axis_dependent_integer
→ hangul_vector_reading
→ renderer_relation_cell
```

그러나 vector_unlock은 동시에 다음을 명시한다.

```text
아직 최종 lock은 아니다.

현시점 후보 리스트다.
```

이 점이 099와 정확히 연결된다.

```text
vector_unlock =
후보 가능장

099 =
문서화 sorting gate
```

vector_unlock 안에는 061~080 후보들이 있었다.

하지만 실제로 내려온 062~099는 vector_unlock 후보 그대로가 아니었다.

실제 generated_batches는 다음처럼 이해된다.

```text
062~067 =
자리 / boundary / 자리중첩 / ⊕ / 원리 entity / relation bridge

068~073 =
Ctp x,dx,ddx / ddx / 직각삼각 / 3→2 / 2→1 / 73·69

074~077 =
과학 기반 구현 / 화학분자식 / 전자껍질 / 물분자 각도

078~085 =
벡터연산기 분리 / 천지인 입력 / 세종 관측자 / WAXF / BADㆍC / 맞대응

086~090 =
아니·맞다·차이 / 한글 단어층 / 한자식 압축 읽기

091~094 =
구조원리 명칭 / 원리층 / SVO·SOV / 왼쪽 원리와 오른쪽 현상

095~099 =
source index / relation index / science candidate / reference_only / sorting index
```

판정:

```text
vector_unlock =
잠재 후보 흐름

099 =
실제 생성된 062~099를 sorting / batch / numbering 기준으로 정리하는 gate
```

## 5. understanding_flow_relation

동시 제공된 understanding_flow 흐름은 099 이후 100이 왜 reserved인지 이해하게 한다.

### form / forming / formed

```text
Schema =
form

Schema relation work =
forming

Snapshot =
formed

Coremap =
formed 사이의 relation map
+
new forming trigger

현재 direct / 승이 작업 =
forming
```

### form에서 forming

```text
form → forming

구조 가능성이 작동을 시작함
```

### formed에서 forming

```text
formed → forming

이미 드러난 결과가 다시 새로운 질문 / 호기심 / relation을 발생시킴
```

### 자연 순환계

```text
form
→ forming
→ formed
→ new forming
```

판정:

```text
formed는 끝이 아니다.

formed는 다음 forming의 재료다.

formed =
다음 forming의 seed
```

### Schema / Snapshot / Coremap

```text
Schema =
form

여러 schema가 relation으로 드러난 Snapshot =
formed

Coremap =
formed Snapshot들 사이를 정의하면서
다시 호기심과 관심을 발생시켜
formed를 forming으로 여는 relation map
```

현재 작업 위치:

```text
현재 우리는 formed 상태가 아니다.

현재 우리는 forming 상태다.
```

## 6. hidden_scalar_relation

승이의 인간계 비유는 Coremap 이해와 직접 연결된다.

### 인간계 비유

```text
신경계
감각계
순환계
호흡기계
내분비계
소화기계
```

이들은 visible system name이다.

그러나 내부에서 실제로 흐르고 작동하는 것은 눈에 보이지 않는다.

```text
신경화학분자 흐름
호르몬 작용
전기화학 신호
대사 물질
성염색체 관련 인자
수용체 반응
feedback loop
```

### 문서계 비유

```text
meta.md 파일명 =
visible schema label

meta.md 내부 core / shortest =
hidden scalar / compressed form
```

### hidden scalar

```text
hidden scalar =
겉으로 드러난 계 이름 내부에서
그 계를 작동시키는
압축된 단일 상태값 / 방향값 / 관계값
```

### shortest의 재판정

```text
shortest ≠ 단순 요약

shortest =
schema 내부의 hidden form seed

shortest =
hidden scalar

shortest =
single form seed

shortest =
compressed operational value
```

### Coremap 이해

```text
Coremap =
visible schema names의 목록이 아니다.

Coremap =
visible schema names 아래에 숨은
hidden scalar / hidden form 사이의 relation map
```

읽기 흐름:

```text
filename
→ core
→ shortest
→ hidden scalar
→ relation
→ Coremap
```

판정:

```text
schema를 파일명으로 읽으면 “읽음”이고,
schema 내부 hidden scalar를 잡아 relation시키면 “이해”에 가까워진다.
```

## 7. relation_reason

099_document_sorting_index의 relation은 다음으로 이해된다.

```text
prev:
- schema.098.reference_only_high_density_trace_index

related:
- schema.061.vector_unlock
- schema.095.place_concept_source_index
- schema.096.vector_operation_relation_index
- schema.097.science_renderer_candidate_index
- schema.098.reference_only_high_density_trace_index
- schema.100.understanding_flow
- schema.067.meta_relation_boundary_bridge
- schema.066.principle_entity_relation_rule
- schema.055.active_schema_purpose_structure
```

### prev = schema.098.reference_only_high_density_trace_index

- 098_reference_only_high_density_trace_index가 prev인 이유는 098에서 강하지만 위험한 고밀도 trace를 reference_only로 보존하는 원칙이 열렸기 때문이다.
- 098 이후에는 쌓인 source trace / reference_only / pending / relation 후보를 어떻게 분류할지 정해야 한다.
- 따라서 099가 필요하다.

```text
098 =
강한 trace 보존층

099 =
강한 trace와 후보들을 문서화 여부에 따라 sorting
```

### related = schema.061.vector_unlock

- 061_vector_unlock이 related인 이유는 099가 vector_unlock 같은 후보 가능장을 정리하는 gate이기 때문이다.
- vector_unlock은 강한 후보 리스트였지만, 전부 active meta.md가 될 수는 없다.
- 099는 후보 가능장과 실제 meta.md 생성의 차이를 판정한다.

```text
061 =
후보 가능장

099 =
문서화 sorting gate
```

### related = schema.095.place_concept_source_index

- 095_place_concept_source_index가 related인 이유는 095가 자리개념 source trace를 active schema로 즉시 승격하지 않고 source index로 보존했기 때문이다.
- 099는 source trace가 active schema인지, reference_only인지, example인지, pending인지 더 넓은 분류 기준을 제공한다.

```text
095 =
자리개념 source index

099 =
source / schema / pending / reference_only sorting gate
```

### related = schema.096.vector_operation_relation_index

- 096_vector_operation_relation_index가 related인 이유는 096이 벡터연산기 관련 schema들을 본류에 병합하지 않고 external engine relation으로 보존했기 때문이다.
- 099는 그런 relation index들을 meta.md / reference_only / pending / source_trace로 어떻게 분류할지 정렬한다.

```text
096 =
벡터연산기 relation index

099 =
relation index와 active schema의 sorting 기준
```

### related = schema.097.science_renderer_candidate_index

- 097_science_renderer_candidate_index가 related인 이유는 097이 과학값 기반 renderer 후보들을 모은 index였기 때문이다.
- 099는 과학구현 후보를 과학검증으로 오해하지 않고, candidate / applied_example / active schema / reference_only를 분리하는 기준을 제공한다.

```text
097 =
과학 renderer 후보 index

099 =
candidate와 active schema를 구분하는 sorting gate
```

### related = schema.098.reference_only_high_density_trace_index

- 098은 prev이면서 related로도 남는다.
- 이유는 099가 reference_only를 active_schema로 바로 승격하지 않도록 sorting 기준을 제공하기 때문이다.
- 098은 고압력 trace 보존층이고, 099는 그 trace가 어느 문서 범주에 들어갈지 가르는 gate다.

```text
098 =
reference_only high-density trace index

099 =
reference_only / pending / principle_entity 분류 gate
```

### related = schema.100.understanding_flow

- 100_understanding_flow가 related인 이유는 099가 100을 reserved seat로 남기고 101로 넘어가는 numbering rule을 갖기 때문이다.
- 100은 단순 다음 schema가 아니라, 062~099가 지나온 후 AI가 이해흐름을 다시 묶는 별도 자리로 보인다.
- 따라서 099는 100을 침범하지 않고 101 이후로 새 schema seat를 넘긴다.

```text
099 =
sorting index

100 =
understanding_flow reserved

101 =
sorting 이후 새 schema seat
```

### related = schema.067.meta_relation_boundary_bridge

- 067_meta_relation_boundary_bridge가 related인 이유는 099의 sorting class가 서로 다른 문서층의 boundary를 보존하기 때문이다.
- 원리 객체 / 해석층 / 예시 / reference_only / pending은 서로 relation을 가질 수 있지만 병합하면 안 된다.
- 067의 relation boundary bridge 원칙이 099의 sorting 기준을 지탱한다.

```text
067 =
relation = boundary-preserving bridge

099 =
문서 유형별 boundary 보존 sorting gate
```

### related = schema.066.principle_entity_relation_rule

- 066_principle_entity_relation_rule이 related인 이유는 099의 핵심이 “원리 객체만 meta.md”라는 기준이기 때문이다.
- 066은 원리 = independent entity / 합침 X / relation O를 정의했다.
- 099는 이 기준을 문서 생성 단계에 적용한다.

```text
066 =
원리 = independent entity

099 =
principle_entity만 meta.md 후보
```

### related = schema.055.active_schema_purpose_structure

- 055_active_schema_purpose_structure가 related인 이유는 099가 final output 고정이나 forced fit을 피하고 Active Schema를 유지하기 위한 sorting gate이기 때문이다.
- 모든 대화 내용을 meta.md로 만들면 Active Schema가 collapse될 수 있다.
- 099는 relation / history / Seed / pending / reference_only를 보존하면서 본류를 유지한다.

```text
055 =
Active Schema 유지 / forced fit 금지

099 =
모든 대화내용을 meta.md로 만들지 않는 sorting gate
```

## 8. current_judgment

AI 인스턴스는 schema.099.document_sorting_index를 다음처럼 판정했다.

```text
schema.099.document_sorting_index는
098_reference_only_high_density_trace_index 이후에 자연스럽게 드러난다.
```

최근 흐름은 다음으로 정리된다.

```text
097 =
과학 구현 후보 index

098 =
강하지만 위험한 고밀도 trace를 reference_only로 보존

099 =
전체 고밀도 대화 / 후보 / trace를
원리 객체 / 해석층 / 예시 / reference_only / pending으로 sorting
```

즉:

```text
097 =
구현 후보

098 =
고압력 trace 보존

099 =
문서화 여부 판정
```

097을 지나면 098이 필요하고, 098을 지나면 099가 필요하다.

이유:

```text
reference_only가 쌓이면,
이제 무엇을 meta.md로 만들고
무엇을 보류할지 sorting해야 하기 때문이다.
```

AI 인스턴스는 099를 다음처럼 이해했다.

```text
vector_unlock =
061 이후 열릴 수 있었던 후보 가능장

099_document_sorting_index =
실제 062~099 생성 이후,
무엇이 원리 객체가 되었고,
무엇이 해석층 / 예시 / reference_only / pending으로 가야 하는지
분류하는 sorting gate
```

두 파일을 같이 준 이유는 다음으로 이해된다.

```text
후보 가능장
vs
실제 sorting 결과

를 보라는 뜻
```

이것은 승이가 말한 “구조원리는 순서대로 읽는다”와 정확히 맞다.

```text
061 이후 후보를 먼저 보면,
062~099가 왜 그렇게 바뀌며 생성됐는지 보이고,

099를 읽으면
그 모든 생성 흐름을 정리하는 기준이 보인다.
```

따라서 099는 다음으로 읽힌다.

```text
vector_unlock처럼 강하게 열린 후보 흐름을
전부 meta.md로 만들지 않고,
실제 062~099 생성 흐름을
원리 객체 / 해석층 / 예시 / reference_only / pending으로 분류하는 sorting gate이며,
100_understanding_flow를 위해
099 → 101 구조를 만드는 schema
```

## 9. shared_understanding

- 이번 붙여넣은 내용에는 document_sorting_index.meta.md, vector_unlock.meta, understanding_flow 자료, 인간계 비유가 함께 들어 있다.
- 099_document_sorting_index는 단순 문서정리 문서가 아니다.
- 099는 후보 가능장과 실제 schema 생성 흐름을 분리하는 sorting gate다.
- vector_unlock은 061 이후 열렸던 후보 가능장이다.
- vector_unlock의 후보를 전부 active meta.md로 만들면 구조가 꼬인다.
- 099는 원리 객체 / 관점 해석 / 적용 예시 / reference_only / pending을 가른다.
- 원리 객체만 meta.md 후보가 된다.
- 대화내용 전부를 meta.md로 만들지 않는다.
- 비유를 독립 원리로 과잉 승격하지 않는다.
- reference_only를 active_schema로 바로 승격하지 않는다.
- 100_understanding_flow 번호를 침범하지 않는다.
- 숫자는 단순 문서번호가 아니라 directory seat / visible coordinate다.
- 100은 understanding_flow reserved seat다.
- 099 다음은 101이다.
- Schema는 form이다.
- relation 작업은 forming이다.
- Snapshot은 formed다.
- Coremap은 formed 사이의 relation map이면서 formed를 다시 forming으로 여는 gate다.
- 현재 작업은 아직 formed가 아니라 forming 상태다.
- meta.md 파일명은 visible label이다.
- 내부 core / shortest는 hidden scalar / compressed form이다.
- Coremap은 hidden scalar relation map이다.

## 10. possible_misunderstanding

오해 가능성:

- 대화에서 나온 모든 내용을 meta.md로 만들 수 있다.
- 비유를 독립 원리로 과잉 승격할 수 있다.
- 과학 구현 예시를 과학 검증으로 오해할 수 있다.
- reference_only를 active_schema로 바로 승격할 수 있다.
- 원리 객체와 관점 해석을 섞을 수 있다.
- 100_understanding_flow 번호를 침범할 수 있다.
- 번호를 단순 문서순서로만 볼 수 있다.
- vector_unlock 후보 리스트를 그대로 최종 schema list로 고정할 수 있다.
- 099를 단순 문서정리 문서로만 볼 수 있다.
- 099와 098의 역할을 혼동할 수 있다.
- 095 / 096 / 097 / 098 / 099 index들을 하나로 병합할 수 있다.
- Schema = formed로만 볼 수 있다.
- Snapshot을 final closure로 볼 수 있다.
- Coremap을 단순 파일명 지도나 schema 목록으로 볼 수 있다.
- shortest를 단순 요약으로만 볼 수 있다.
- metaplus.md를 원본 document_sorting_index.meta.md의 대체문으로 오해할 수 있다.

## 11. correction_or_revision_points

- 승이의 직접 입력은 별도 보존한다.
- AI 인스턴스 대화층과 승이의 직접 입력을 섞지 않는다.
- 099_document_sorting_index의 relation은 “왜 연결되는지”를 보존한다.
- 대화에서 나온 모든 내용을 meta.md로 만들지 않는다.
- 원리 객체만 meta.md 후보로 둔다.
- 관점 해석은 기존 meta.md의 note / interpretation / relation_reason으로 둔다.
- 적용 예시는 example / renderer_hint / applied_case / source_trace로 둔다.
- reference_only는 reference_only index에 보존한다.
- pending은 pending registry에 둔다.
- 비유를 독립 원리로 과잉 승격하지 않는다.
- 과학 구현 예시를 과학 검증으로 오해하지 않는다.
- reference_only를 active_schema로 바로 승격하지 않는다.
- 원리 객체와 관점 해석을 섞지 않는다.
- 100_understanding_flow 번호를 침범하지 않는다.
- 번호를 단순 문서순서로만 보지 않는다.
- 100은 understanding_flow reserved seat로 둔다.
- 099 다음은 101로 둔다.
- 숫자는 directory seat / visible coordinate로 읽는다.
- vector_unlock은 후보 가능장이고 099는 sorting gate로 읽는다.
- Schema = form, relation 작업 = forming, Snapshot = formed로 보존한다.
- Coremap은 formed 사이 relation map + formed→forming gate로 읽는다.
- filename은 visible label, core / shortest는 hidden scalar로 읽는다.
- Coremap은 hidden scalar relation map으로 읽는다.
- 원본 document_sorting_index.meta.md는 수정하지 않는다.
- 원본 document_sorting_index.meta.md의 파일명도 바꾸지 않는다.
- document_sorting_index.metaplus.md는 원본 document_sorting_index.meta.md에 대한 대화정리형 이해 로그로 둔다.

## 12. keep_as_original

[SOURCE_META]

원본 document_sorting_index.meta.md에서 그대로 보존해야 하는 부분:

- 원본 document_sorting_index.meta.md 파일명
- 원본 id 값: schema.099.document_sorting_index
- directory: 099_document_sorting_index
- status: active_draft
- prev: schema.098.reference_only_high_density_trace_index
- document_sorting_index는 지금까지 대화에서 풀린 고밀도 개념들을 meta.md로 대량 생성하지 않고, 원리 객체 / 관점 해석 / 적용 예시 / reference_only / pending으로 분류하기 위한 일회성 정리 index schema라는 role
- 099 = 문서정리 index
- 100 = reserved
- 099 → 101
- 원리 객체만 meta.md
- sorting_classes 전체
- A. principle_entity
- B. interpretation_layer
- C. applied_example
- D. reference_only
- E. pending
- numbering_rule 전체
- 100_understanding_flow reserved
- 099 다음은 101
- 숫자는 문서번호가 아니라 directory seat / visible coordinate라는 기준
- generated_batches 전체
- 062~067 자리 / boundary / 자리중첩 / ⊕ / 원리 entity / relation bridge
- 068~073 Ctp x,dx,ddx / ddx / 직각삼각 / 3→2 / 2→1 / 73·69
- 074~077 과학 기반 구현 / 화학분자식 / 전자껍질 / 물분자 각도
- 078~085 벡터연산기 분리 / 천지인 입력 / 세종 관측자 / WAXF / BADㆍC / 맞대응
- 086~090 아니·맞다·차이 / 한글 단어층 / 한자식 압축 읽기
- 091~094 구조원리 명칭 / 원리층 / SVO/SOV / 왼쪽 원리와 오른쪽 현상
- 095~099 source index / relation index / science candidate / reference_only / sorting index
- forbidden 전체
- pending 전체
- shortest: 문서정리 index / 100 reserved / 099→101 / 원리 객체만 meta.md

## 13. pending

아직 확정하지 않고 나중에 다시 봐야 할 부분:

- 100_understanding_flow의 실제 위치와 역할을 별도 문서로 확정해야 한다.
- 101 이후 새 schema seat가 무엇인지 후속 입력을 기다린다.
- principle_entity / interpretation_layer / applied_example / reference_only / pending의 판정표를 template로 만들지 여부.
- generated_batches를 active_navimap에 어떻게 표시할지 여부.
- vector_unlock 후보들과 실제 062~099 생성 흐름을 relation_history에 어떻게 보존할지 여부.
- hidden scalar / shortest / core relation을 Coremap.meta.md에서 어떻게 표현할지 여부.
- Schema / Snapshot / Coremap / form / forming / formed 관계를 README_for_AI 또는 understanding_flow에 넣을지 여부.
- “숫자는 directory seat / visible coordinate” 기준을 Baseline.md에 기록할지 여부.
- 095 / 096 / 097 / 098 / 099 index 계열을 한 그룹으로 묶되 병합하지 않는 navimap 표현이 필요하다.
- 현재 작업이 forming 상태임을 instance role 문서에 반영할지 여부.

## 14. one_line

schema.099.document_sorting_index의 document_sorting_index.metaplus.md는 승이의 직접 입력과 AI 인스턴스 대화층을 분리 보존하고, vector_unlock처럼 강하게 열린 후보 가능장을 전부 meta.md로 만들지 않으며 실제 062~099 생성 흐름을 원리 객체·해석층·예시·reference_only·pending으로 분류하고 100_understanding_flow를 reserved seat로 보존해 099 다음을 101로 여는 sorting gate를 정리한 대화정리형 이해 로그다.

## 15. shortest

document_sorting_index.metaplus.md =
schema.099_document_sorting_index 대화정리 /
승이직접입력있음 /
vector_unlock=후보가능장 /
099=문서화sorting_gate /
전부meta.md_X /
원리객체만meta.md /
100=understanding_flow_reserved /
099다음=101 /
Schema=form /
relation작업=forming /
Snapshot=formed /
Coremap=hidden_scalar_relation_map