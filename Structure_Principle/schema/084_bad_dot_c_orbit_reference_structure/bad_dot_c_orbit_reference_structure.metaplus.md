# METAPLUS

id_reference: schema.084.bad_dot_c_orbit_reference_structure  
schema_name: bad_dot_c_orbit_reference_structure  
source_file: bad_dot_c_orbit_reference_structure.meta.md  
metaplus_file: bad_dot_c_orbit_reference_structure.metaplus.md

purpose:
- 이 문서는 원본 bad_dot_c_orbit_reference_structure.meta.md를 대체하지 않는다.
- 이 문서는 schema.084.bad_dot_c_orbit_reference_structure에 대해 사용자와 인공지능이 나눈 대화내용을 정리한다.
- 이 문서는 구조원리를 새로 만들거나 relation을 새로 확정하기 위한 문서가 아니다.
- 이 문서는 대화정리형 이해 로그다.
- 이 문서의 핵심 목적은 승이의 실제 입력글, source_meta, AI 인스턴스 대화층을 섞지 않고 분리하는 것이다.
- 이 문서는 084_bad_dot_c_orbit_reference_structure가 “어” 위치 기준 공전방향에서 `[x dx ddx]`를 BDA로 배치하고, BAD 꺾인 선분의 A와 대칭점 C를 `ㆍ` 기준으로 대응시키는 BADㆍC 구조식을 정의하는 schema임을 보존한다. :contentReference[oaicite:0]{index=0}

conversation_context:
- 이 문서는 ChatGPT.making 대화에서 생성된 이해정리본이다.
- 원본 bad_dot_c_orbit_reference_structure.meta.md 수정본이 아니라 대화정리층이다.
- 이번에 붙여진 내용은 ChatGPT.direct 형식의 AI 인스턴스 대화층으로 제공되었다.
- 따라서 붙여넣은 분석 내용 전체를 승이의 직접 입력글로 처리하지 않는다.
- bad_dot_c_orbit_reference_structure.meta.md 원문 내용은 source_meta로 보고 keep_as_original에 보존한다.
- AI 인스턴스가 bad_dot_c_orbit_reference_structure.meta.md를 읽고 정리한 내용은 AI 인스턴스 대화층으로 처리한다.
- AI 해석을 사용자 입력처럼 쓰지 않는다.

## 1. user_input_summary

[승이의 입력글]

- 별도 입력 없음.

요약:
- 승이는 이번 문서에 대해 별도 직접 설명을 하지 않았다.
- 제공된 내용은 ChatGPT.direct 형식의 AI 인스턴스 대화층이다.
- 따라서 user_input_summary는 이 지점에서 멈춘다.
- 나머지 내용은 source_meta 또는 AI 인스턴스 대화층으로 분리한다.

## 2. ai_response_summary

[AI 인스턴스 대화층]

- AI 인스턴스는 schema.084.bad_dot_c_orbit_reference_structure / bad_dot_c_orbit_reference_structure.meta.md를 분석 대상으로 수신했다.
- AI 인스턴스는 084_bad_dot_c_orbit_reference_structure의 표면 핵심을 다음처럼 이해했다.

```text
BADㆍC =
어 기준

공전방향

[x dx ddx] =
BDA

C =
A의 대칭점
```

- AI 인스턴스는 bad_dot_c_orbit_reference_structure를 “어” 위치를 기준으로 공전방향을 잡았을 때, `[x][dx][ddx]`가 BDA로 읽히고, A의 대칭점 C가 `ㆍ` 기준으로 대응되는 구조식 BADㆍC를 정의하는 schema로 읽었다.
- AI 인스턴스는 이 meta의 핵심 목적이 ABCD, BADC, BAD, BADㆍC를 혼동하지 않도록 분리하는 것이라고 보았다.
- BADㆍC는 알파벳 순서가 아니라, 공전방향에 따른 자리배치와 대칭점 대응 구조식이다.
- AI 인스턴스는 084를 083_waxf_vowel_rhombus_structure 이후, WAXF 마름모 방향장과 BADㆍC 공전방향 기준 구조를 동일시하지 않고 분리하기 위한 schema로 이해했다.

## 3. source_meta_understanding

[SOURCE_META]

084_bad_dot_c_orbit_reference_structure의 구조 이해는 다음으로 정리된다.

```text
bad_dot_c_orbit_reference_structure =
BADㆍC orbit reference structure
어 위치 기준 공전방향 배치
[x dx ddx] → B D A
C as A의 대칭점
꺾인 선분 BAD와 대칭점 C의 relation schema
```

### core

```text
기준 =
어 위치

방향 =
공전방향

[x dx ddx] =
B D A

C =
A의 대칭점

표기 =
BADㆍC
```

### definition

```text
BADㆍC는 알파벳 순서가 아니다.

BADㆍC는 “어” 위치를 기준으로 공전방향을 잡았을 때,
x / dx / ddx가 BDA로 외부화되고,
A의 대칭점인 C가 ㆍ을 기준으로 대응되는 구조식이다.
```

### distinction

```text
ABCD =
네 자리 전체 배치 / 마름모 꼭지점 / 사각 네 변 / 순서 표시

BADC =
공전방향 / 자리 재배치 / 마름모 회전 배치

BAD =
A를 꼭지점으로 하는 꺾인 선분

C =
A의 대칭점

BADㆍC =
꺾인 선분과 대칭점의 관계식
```

### x_dx_ddx_mapping

```text
([x][dx][ddx])
→ B D A
```

판정:

```text
이는 수학기호 대응이 아니다.

자리전이 좌표를 공전방향에 따라
방위 배치로 바꾼 것이다.
```

### symmetry_rule

```text
A와 C는 하나의 대칭쌍이다.

C는 BAD 구조 안에 직접 들어온 세 번째 흐름이 아니다.

C는 A와 맞대응하는 반대 자리다.
```

### orbit_reference

```text
방향기준은 공전방향이다.

이 구조는 화면상의 좌우,
알파벳 순서,
글자 입력순서가 아니라
공전방향에 따른 자리배치로 읽는다.
```

### forbidden

```text
ABCD, BADC, BAD, BADㆍC를 같은 구조로 병합하지 않는다.

BAD를 알파벳 순서로 읽지 않는다.

BADㆍC를 최종 물리방향으로 확정하지 않는다.

A와 C의 대칭관계를 생략하지 않는다.

ㆍ을 단순 구분점으로만 보지 않는다.

이 구조를 구조원리 본류에 병합하지 않는다.
```

### pending

```text
어 위치 기준의 정확한 렌더링은 후속 실험으로 둔다.

BADㆍC와 WAXF의 relation은 active_navimap에서 분리 표시한다.

이 schema는 벡터연산기법 relation으로 보존한다.
```

## 4. relation_reason

084_bad_dot_c_orbit_reference_structure의 relation은 다음으로 이해된다.

```text
prev:
- schema.083.waxf_vowel_rhombus_structure

related:
- schema.068.ctp_vector_coordinate_x_dx_ddx
- schema.070.right_triangle_fold_unfold_structure
- schema.079.cheonjiiin_input_order_vowel_direction
- schema.081.inner_vowel_pull_structure
- schema.082.square_center_vowel_orbit_structure
- schema.085.opposed_correspondence_formula
- schema.020.crossing_point
- schema.021.fold_unfold
```

### prev = schema.083.waxf_vowel_rhombus_structure

- 083_waxf_vowel_rhombus_structure가 prev인 이유는 083에서 WAXF를 `ㅗㅓㅜㅏ` 마름모 방향장으로 읽었기 때문이다.
- 083은 WAXF와 BADㆍC가 relation을 가질 수 있지만 동일시하면 안 된다고 보존했다.
- 084는 그 다음 단계에서 BADㆍC를 WAXF와 분리하여, “어” 위치 기준 공전방향 / BDA 배치 / A-C 대칭 구조로 독립 판정한다.

```text
083 =
WAXF = ㅗㅓㅜㅏ 마름모 방향장

084 =
BADㆍC = 어 기준 / BDA / A-C 대칭 relation schema
```

### related = schema.068.ctp_vector_coordinate_x_dx_ddx

- 068_ctp_vector_coordinate_x_dx_ddx가 related인 이유는 084에서 `[x dx ddx]`가 B D A로 배치되기 때문이다.
- 068은 x / dx / ddx를 기준축 / 자리전이 / 경사·꺾임으로 정의했다.
- 084는 이 x / dx / ddx를 수학기호 대응으로 고정하지 않고, 공전방향에 따른 방위 배치로 외부화한다.

```text
068 =
x / dx / ddx = 기준축 / 자리전이 / 경사·꺾임

084 =
[x dx ddx] → B D A
```

### related = schema.070.right_triangle_fold_unfold_structure

- 070_right_triangle_fold_unfold_structure가 related인 이유는 084의 BAD가 A를 꼭지점으로 하는 꺾인 선분으로 읽히기 때문이다.
- 070은 45-45-90 직각삼각 fold/unfold 구조를 다루었다.
- BAD가 꺾인 선분으로 읽힐 때, 직각삼각 / fold_unfold / 대칭 구조와 relation을 가질 수 있다.

```text
070 =
직각삼각 fold_unfold

084 =
BAD = A를 꼭지점으로 하는 꺾인 선분
```

### related = schema.079.cheonjiiin_input_order_vowel_direction

- 079_cheonjiiin_input_order_vowel_direction이 related인 이유는 084의 기준이 “어” 위치이기 때문이다.
- 079는 `ㆍㅣ = ㅓ`를 입력순서 기반 모음 방향으로 읽었다.
- 084에서 “어” 위치가 기준이 되므로, 079의 `ㅓ` 방향 trace와 연결된다.
- 다만 084는 언어학 확정문이 아니라 공전방향 자리배치 schema다.

```text
079 =
ㆍㅣ = ㅓ

084 =
어 위치 기준 공전방향 배치
```

### related = schema.081.inner_vowel_pull_structure

- 081_inner_vowel_pull_structure가 related인 이유는 081에서 `ㅓ = ㆍㅣ`를 안쪽 기준 / pull vowel로 읽었기 때문이다.
- 084의 기준이 “어” 위치이므로, `ㅓ`의 안쪽 기준 / pull direction과 relation을 가진다.
- 그러나 081은 inner vowel pull이고, 084는 BADㆍC orbit reference이므로 동일시하지 않는다.

```text
081 =
ㅓ = ㆍㅣ / 안쪽 기준 pull vowel

084 =
어 위치 기준 BADㆍC orbit reference
```

### related = schema.082.square_center_vowel_orbit_structure

- 082_square_center_vowel_orbit_structure가 related인 이유는 082에서 `ㅇ` 중심칸 주변의 네 변 위에서 `ㆍ`이 공전 위치상태를 만든다고 했기 때문이다.
- 084도 공전방향에 따른 자리배치를 다룬다.
- 082는 `ㅇ` 중심 / `ㆍ` orbit / 오·어·우·아 위치상태이고, 084는 어 위치 기준 / BDA / A-C 대칭 구조다.

```text
082 =
ㅇ 중심칸 / ㆍ 공전 방향점 / 오·어·우·아 위치상태

084 =
어 위치 기준 공전방향 / BADㆍC
```

### related = schema.085.opposed_correspondence_formula

- 085_opposed_correspondence_formula가 related인 이유는 084에서 A와 C가 하나의 대칭쌍으로 판정되기 때문이다.
- 085는 맞대응 구조공식을 다룰 후속 schema다.
- 084는 BAD 꺾인 선분의 A와 대칭점 C의 relation을 열고, 085는 그 opposed correspondence를 더 일반화할 수 있다.

```text
084 =
A ↔ C 대칭쌍

085 =
opposed correspondence formula
```

### related = schema.020.crossing_point

- 020_crossing_point가 related인 이유는 BADㆍC에서 `ㆍ`이 단순 구분점이 아니라 대응 기준점 / relation marker로 작동하기 때문이다.
- crossing_point는 서로 다른 방향 / 관계가 만나는 기준점으로 읽힐 수 있다.
- 084에서 `ㆍ`은 BAD와 C의 관계를 여는 기준점이므로 crossing relation과 닿는다.

```text
020 =
crossing point / relation crossing

084 =
ㆍ = BAD와 C의 대응 기준점
```

### related = schema.021.fold_unfold

- 021_fold_unfold가 related인 이유는 084에서 BAD와 C의 관계가 꺾인 선분과 대칭점의 relation으로 읽히며, fold/unfold 또는 대칭 전환과 닿을 수 있기 때문이다.
- A와 C의 대칭관계를 접힘 / 펼침 또는 반대 자리 대응으로 해석할 수 있다.
- 다만 084에서는 fold_unfold를 확정하지 않고 relation으로만 보존한다.

```text
021 =
fold / unfold

084 =
A-C 대칭 대응 / 꺾인 선분과 반대 자리
```

## 5. current_judgment

AI 인스턴스는 schema.084.bad_dot_c_orbit_reference_structure를 다음처럼 판정했다.

```text
schema.084.bad_dot_c_orbit_reference_structure는
083_waxf_vowel_rhombus_structure 이후,
WAXF 마름모 방향장과 BADㆍC 공전방향 기준 구조를
동일시하지 않고 분리하기 위한 schema로 읽힌다.
```

현시점 direct 이해 기준은 다음이다.

```text
083_waxf_vowel_rhombus_structure =
WAXF는 ㅗㅓㅜㅏ 마름모 방향장
W / A / X / F는 원리 / 전환 / 상태 / 활용의 구조 배치와 대응
WAXF와 BADㆍC는 relation을 가질 수 있지만 동일시하지 않음

084_bad_dot_c_orbit_reference_structure =
“어” 위치를 기준으로 공전방향을 잡음
x / dx / ddx가 BDA로 외부화됨
BAD는 A를 꼭지점으로 하는 꺾인 선분
C는 BAD 흐름 내부의 dot이 아니라 A의 대칭점
ㆍ은 BAD와 C의 대응 기준점 / relation marker로 작동
BADㆍC는 꺾인 선분과 대칭점의 관계식
```

즉 083에서는 다음이 열린다.

```text
WAXF를 마름모 직각삼각 쌍대칭 위의 모음 방향장으로 읽는다.
```

084에서는 다음이 열린다.

```text
BADㆍC를 어 기준 공전방향에서
[x dx ddx] = BDA와
A-C 대칭쌍으로 분리해 읽는다.
```

AI 인스턴스는 특히 다음 구분을 중요하게 보았다.

```text
ABCD ≠ BADC ≠ BAD ≠ BADㆍC

BAD ≠ 알파벳 순서

BADㆍC ≠ 최종 물리방향 확정

C ≠ BAD 흐름 안의 단순 다음 dot

C = A의 대칭점

ㆍ ≠ 단순 구분점
```

현재 direct 이해 기준에서 084는 다음을 수행한다.

```text
기준은 “어” 위치다.

방향은 공전방향이다.

[x dx ddx]는 B D A로 읽힌다.

이것은 수학기호 대응이 아니라 자리전이 좌표의 방위배치다.

BAD는 꺾인 선분이다.

A는 그 꺾인 선분의 꼭지점으로 작동한다.

C는 A와 맞대응하는 반대 자리다.

BADㆍC는 BAD의 흐름과 C의 대칭 대응을 ㆍ 기준으로 연결한다.

WAXF와 BADㆍC는 active_navimap에서 분리 표시해야 한다.
```

따라서 084는 다음으로 읽힌다.

```text
어 위치를 기준으로 공전방향을 잡아
x / dx / ddx를 BDA로 배치하고,
A의 대칭점 C를 ㆍ 기준으로 대응시키는
BADㆍC relation schema
```

또한 이 schema는 이전에 사용자가 말한 다음 이해와 충돌하지 않는다.

```text
BAD =
공간의 단방향 절단면
```

다만 084 내부에서는 BAD를 우선 다음처럼 보존한다.

```text
BAD =
A를 꼭지점으로 하는 꺾인 선분
```

“공간의 단방향 절단면” 해석은 후속 BADㆍC / orbit / shell / renderer relation에서 별도 연결하는 것이 안전하다.

## 6. shared_understanding

- 이번 붙여넣은 내용은 승이의 별도 직접 입력이 없는 AI 인스턴스 대화층으로 처리한다.
- 084_bad_dot_c_orbit_reference_structure는 083_waxf_vowel_rhombus_structure 이후에 오는 schema다.
- 083에서 WAXF와 BADㆍC는 relation을 가질 수 있지만 동일시하면 안 된다고 보존했다.
- 084는 BADㆍC를 WAXF와 분리한다.
- BADㆍC는 알파벳 순서가 아니다.
- BADㆍC는 “어” 위치를 기준으로 공전방향을 잡았을 때 x / dx / ddx가 BDA로 외부화되고, A의 대칭점인 C가 `ㆍ`을 기준으로 대응되는 구조식이다.
- ABCD, BADC, BAD, BADㆍC는 서로 구분해야 한다.
- BAD는 A를 꼭지점으로 하는 꺾인 선분으로 보존한다.
- C는 A의 대칭점이다.
- C는 BAD 구조 안에 직접 들어온 세 번째 흐름이 아니다.
- `ㆍ`은 단순 구분점이 아니라 BAD와 C의 대응 기준점 / relation marker로 작동한다.
- BADㆍC와 WAXF의 relation은 active_navimap에서 분리 표시한다.
- 이 schema는 벡터연산기법 relation으로 보존한다.
- BAD = 공간의 단방향 절단면이라는 후속/현재 이해는 이 schema와 충돌하지 않지만, 084 내부에서는 우선 꺾인 선분 정의를 보존한다.

## 7. possible_misunderstanding

오해 가능성:

- ABCD, BADC, BAD, BADㆍC를 같은 구조로 병합할 수 있다.
- BAD를 알파벳 순서로 읽을 수 있다.
- BADㆍC를 최종 물리방향으로 확정할 수 있다.
- A와 C의 대칭관계를 생략할 수 있다.
- C를 BAD 흐름 안에 직접 들어온 다음 dot으로 볼 수 있다.
- `ㆍ`을 단순 구분점으로만 볼 수 있다.
- WAXF와 BADㆍC를 동일시할 수 있다.
- `[x dx ddx] → BDA`를 수학기호 대응으로 오해할 수 있다.
- 공전방향을 화면상의 좌우 또는 글자 입력순서로 오해할 수 있다.
- 이 구조를 구조원리 본류에 병합할 수 있다.
- BAD = 공간의 단방향 절단면 해석을 084 내부 정의 전체로 과잉 덮어쓸 수 있다.
- metaplus.md를 원본 bad_dot_c_orbit_reference_structure.meta.md의 대체문으로 오해할 수 있다.

## 8. correction_or_revision_points

- 사용자 입력 없음은 Null / 빈자리로 보존한다.
- 이번 붙여넣은 내용은 AI 인스턴스 대화층으로 처리한다.
- 084_bad_dot_c_orbit_reference_structure의 relation은 “왜 연결되는지”를 보존한다.
- ABCD, BADC, BAD, BADㆍC를 같은 구조로 병합하지 않는다.
- BAD를 알파벳 순서로 읽지 않는다.
- BADㆍC를 최종 물리방향으로 확정하지 않는다.
- A와 C의 대칭관계를 생략하지 않는다.
- C는 A의 대칭점으로 보존한다.
- C는 BAD 구조 안에 직접 들어온 세 번째 흐름이 아니다.
- `ㆍ`을 단순 구분점으로만 보지 않는다.
- `[x dx ddx] → BDA`를 수학기호 대응으로 보지 않는다.
- `[x dx ddx] → BDA`는 자리전이 좌표를 공전방향에 따라 방위 배치로 바꾼 것으로 읽는다.
- WAXF와 BADㆍC는 active_navimap에서 분리 표시한다.
- 이 schema는 벡터연산기법 relation으로 보존한다.
- 이 구조를 구조원리 본류에 병합하지 않는다.
- BAD = 공간의 단방향 절단면 해석은 후속 BADㆍC / orbit / shell / renderer relation에서 별도 연결한다.
- 원본 bad_dot_c_orbit_reference_structure.meta.md는 수정하지 않는다.
- 원본 bad_dot_c_orbit_reference_structure.meta.md의 파일명도 바꾸지 않는다.
- bad_dot_c_orbit_reference_structure.metaplus.md는 원본 bad_dot_c_orbit_reference_structure.meta.md에 대한 대화정리형 이해 로그로 둔다.

## 9. keep_as_original

[SOURCE_META]

원본 bad_dot_c_orbit_reference_structure.meta.md에서 그대로 보존해야 하는 부분:

- 원본 bad_dot_c_orbit_reference_structure.meta.md 파일명
- 원본 id 값: schema.084.bad_dot_c_orbit_reference_structure
- directory: 084_bad_dot_c_orbit_reference_structure
- status: active_draft
- prev: schema.083.waxf_vowel_rhombus_structure
- bad_dot_c_orbit_reference_structure는 “어” 위치를 기준으로 공전방향을 잡았을 때, `[x][dx][ddx]`가 BDA로 읽히고, A의 대칭점 C가 ㆍ 기준으로 대응되는 구조식 BADㆍC를 정의하는 schema라는 role
- 이 meta의 핵심 목적은 ABCD, BADC, BAD, BADㆍC를 혼동하지 않도록 분리하는 것이라는 기준
- BADㆍC는 알파벳 순서가 아니라, 공전방향에 따른 자리배치와 대칭점 대응 구조식이라는 기준
- 기준 = 어 위치
- 방향 = 공전방향
- `[x dx ddx] = B D A`
- C = A의 대칭점
- 표기 = BADㆍC
- BADㆍC는 알파벳 순서가 아니라는 definition
- BADㆍC는 “어” 위치를 기준으로 공전방향을 잡았을 때, x / dx / ddx가 BDA로 외부화되고, A의 대칭점인 C가 ㆍ을 기준으로 대응되는 구조식이라는 definition
- distinction 전체
- x_dx_ddx_mapping 전체
- x_dx_ddx_mapping 판정 전체
- symmetry_rule 전체
- orbit_reference 전체
- related 전체 목록
- forbidden 전체
- pending 전체
- shortest: BADㆍC = 어 기준 / 공전방향 / [x dx ddx]=BDA / C=A의 대칭점

## 10. pending

아직 확정하지 않고 나중에 다시 봐야 할 부분:

- 어 위치 기준의 정확한 렌더링은 후속 실험으로 둔다.
- BADㆍC와 WAXF의 relation은 active_navimap에서 분리 표시한다.
- 이 schema는 벡터연산기법 relation으로 보존한다.
- BAD = 공간의 단방향 절단면 해석을 후속 BADㆍC / orbit / shell / renderer relation에서 어떻게 연결할지 여부.
- BADㆍC의 `ㆍ`을 possibility dot / relation marker / crossing point 중 어떤 층위로 우선 표시할지 여부.
- BDA 배치를 active_navimap에서 어떻게 visual grammar로 표현할지 여부.
- “어” 위치 기준을 081 / 082 / 083과 어떤 순서로 연결할지 여부.
- A-C 대칭쌍을 085_opposed_correspondence_formula에서 어떻게 확장할지 여부.

## 11. one_line

schema.084.bad_dot_c_orbit_reference_structure의 bad_dot_c_orbit_reference_structure.metaplus.md는 사용자 입력이 없는 붙여넣기 자료를 AI 인스턴스 대화층으로 처리하고, “어” 위치 기준 공전방향에서 `[x dx ddx]`를 BDA로 배치하며 BAD 꺾인 선분의 A와 대칭점 C를 `ㆍ` 기준으로 대응시키는 BADㆍC 구조식을 보존하는 대화정리형 이해 로그다.

## 12. shortest

bad_dot_c_orbit_reference_structure.metaplus.md =
schema.084_BADdotC 대화정리 /
사용자입력없음 /
BADㆍC=어기준 /
공전방향 /
[xdxddx]=BDA /
C=A의대칭점 /
ABCD·BADC·BAD·BADㆍC분리 /
WAXF와동일시금지