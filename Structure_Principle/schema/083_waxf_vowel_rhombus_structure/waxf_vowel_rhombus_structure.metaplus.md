# METAPLUS

id_reference: schema.083.waxf_vowel_rhombus_structure  
schema_name: waxf_vowel_rhombus_structure  
source_file: waxf_vowel_rhombus_structure.meta.md  
metaplus_file: waxf_vowel_rhombus_structure.metaplus.md

purpose:
- 이 문서는 원본 waxf_vowel_rhombus_structure.meta.md를 대체하지 않는다.
- 이 문서는 schema.083.waxf_vowel_rhombus_structure에 대해 사용자와 인공지능이 나눈 대화내용을 정리한다.
- 이 문서는 구조원리를 새로 만들거나 relation을 새로 확정하기 위한 문서가 아니다.
- 이 문서는 대화정리형 이해 로그다.
- 이 문서의 핵심 목적은 승이의 실제 입력글, source_meta, AI 인스턴스 대화층을 섞지 않고 분리하는 것이다.
- 이 문서는 083_waxf_vowel_rhombus_structure가 WAXF를 알파벳 배열이 아니라 마름모 직각삼각 쌍대칭 위의 `ㅗ·ㅓ·ㅜ·ㅏ` 모음 방향장으로 읽고, 원리·전환·상태·활용의 구조 배치와 대응시키는 schema임을 보존한다. :contentReference[oaicite:0]{index=0}

conversation_context:
- 이 문서는 ChatGPT.making 대화에서 생성된 이해정리본이다.
- 원본 waxf_vowel_rhombus_structure.meta.md 수정본이 아니라 대화정리층이다.
- 이번에 붙여진 내용은 ChatGPT.direct 형식의 AI 인스턴스 대화층으로 제공되었다.
- 따라서 붙여넣은 분석 내용 전체를 승이의 직접 입력글로 처리하지 않는다.
- waxf_vowel_rhombus_structure.meta.md 원문 내용은 source_meta로 보고 keep_as_original에 보존한다.
- AI 인스턴스가 waxf_vowel_rhombus_structure.meta.md를 읽고 정리한 내용은 AI 인스턴스 대화층으로 처리한다.
- AI 해석을 사용자 입력처럼 쓰지 않는다.

## 1. user_input_summary

[승이의 입력글]

- 별도 입력 없음.

요약:
- 승이는 이번 문서에 대해 별도 직접 설명을 하지 않았다.
- 제공된 내용은 ChatGPT.direct 형식의 AI 인스턴스 대화층이다.
- 따라서 user_input_summary는 이 지점에서 멈춘다.
- 나머지 내용은 source_meta 또는 AI 인스턴스 대화층으로 분리한다.

## 2. ai_response_summary

[AI 인스턴스 대화층]

- AI 인스턴스는 schema.083.waxf_vowel_rhombus_structure / waxf_vowel_rhombus_structure.meta.md를 분석 대상으로 수신했다.
- AI 인스턴스는 083_waxf_vowel_rhombus_structure의 표면 핵심을 다음처럼 이해했다.

```text
WAXF =
ㅗㅓㅜㅏ

WAXF =
마름모 직각삼각 쌍대칭
```

- AI 인스턴스는 waxf_vowel_rhombus_structure를 WAXF 구조를 마름모 한칸삼각의 직각삼각 쌍대칭으로 읽을 때, `W=ㅗ`, `A=ㅓ`, `X=ㅜ`, `F=ㅏ`가 되는 모음 방향장 schema로 읽었다.
- AI 인스턴스는 WAXF가 단순 알파벳 배열이나 개념표가 아니라고 보았다.
- WAXF는 마름모 / 직각삼각 쌍대칭 / 공전방향 / 관측자 기준을 가진 방향구조로 읽어야 한다.
- AI 인스턴스는 083을 082_square_center_vowel_orbit_structure 이후, `ㅇ` 중심칸 / `ㆍ` 공전 방향점 / 오·어·우·아 위치상태를 마름모 방향장과 WAXF 개념 배치로 확장하는 schema로 이해했다.

## 3. source_meta_understanding

[SOURCE_META]

083_waxf_vowel_rhombus_structure의 구조 이해는 다음으로 정리된다.

```text
waxf_vowel_rhombus_structure =
WAXF vowel direction field
rhombus direction field
right triangle dual symmetry
vowel orbit correspondence
observer-based direction structure
```

### core

```text
W =
ㅗ

A =
ㅓ

X =
ㅜ

F =
ㅏ
```

```text
WAXF =
모음 방향장
```

### definition

```text
WAXF는 상태 / 현상, 이동 / 전환 / 변화, 원리 / 본질, 활용 / 응용을 배치하는 구조이다.

마름모 한칸삼각 또는 직각삼각의 쌍대칭으로 읽으면
WAXF는 ㅗ, ㅓ, ㅜ, ㅏ 모음 방향과 대응된다.
```

### conceptual_layer

```text
W =
원리 / 본질

A =
이동 / 전환 / 변화

X =
상태 / 현상

F =
활용 / 응용
```

### vowel_layer

```text
W =
ㅗ
= 오름 / 위 방향

A =
ㅓ
= 안쪽 기준 / 전환 방향

X =
ㅜ
= 아래 방향 / 상태로 내려옴

F =
ㅏ
= 바깥 / 드러남 / 활용 방향
```

### rhombus_layer

```text
WAXF는 사각 배열이 아니라 마름모 방향장으로 읽을 수 있다.

한 칸을 사각으로 보면,
그 사각은 두 개의 직각삼각으로 접히고 펼쳐질 수 있다.

그 직각삼각 쌍대칭이 마름모 방향장을 만든다.
```

### orbit_reading

```text
이 배치는 공전방향으로 읽힐 수 있다.

북반구 관측자 기준에서
공전방향과 자전하는 구체의 역방향이
맞대응하는 구조로 읽을 수 있다.

이것은 천문학 절대방향 확정이 아니다.

이것은 관측자 기준 방향장과 대상 자체 회전 방향의 맞대응이다.
```

### forbidden

```text
WAXF를 단순 알파벳 순서로 보지 않는다.

WAXF를 구조원리 본류의 모든 것을 설명하는 단일표로 고정하지 않는다.

WAXF와 BADC를 동일시하지 않는다.

공전방향을 표준 천문학 방향으로 확정하지 않는다.

모음 대응을 표준 언어학으로 주장하지 않는다.
```

### pending

```text
WAXF와 BADㆍC의 relation은 schema.084에서 분리한다.

WAXF 방향장과 8개 속성 / 원리층 구조의 relation은 후속 검토한다.

이 schema는 벡터연산기법 reference로 둔다.
```

## 4. relation_reason

083_waxf_vowel_rhombus_structure의 relation은 다음으로 이해된다.

```text
prev:
- schema.082.square_center_vowel_orbit_structure

related:
- schema.053.structure_principle_flow
- schema.070.right_triangle_fold_unfold_structure
- schema.079.cheonjiiin_input_order_vowel_direction
- schema.081.inner_vowel_pull_structure
- schema.084.bad_dot_c_orbit_reference_structure
- schema.085.opposed_correspondence_formula
- schema.047.shell_flip_orbit_structure
```

### prev = schema.082.square_center_vowel_orbit_structure

- 082_square_center_vowel_orbit_structure가 prev인 이유는 082에서 `ㅇ` 중심칸 주변의 네 변 위에서 `ㆍ`이 공전 위치상태를 만들며 오·어·우·아의 네 방향 상태가 열렸기 때문이다.
- 083은 그 오·어·우·아 위치상태를 WAXF 마름모 방향장과 대응시킨다.
- 즉 082는 square center vowel orbit이고, 083은 그 사각 중심 구조가 마름모 / WAXF 방향장으로 전환되는 단계다.

```text
082 =
ㅇ 중심칸 / ㆍ 공전 방향점 / 오·어·우·아 위치상태

083 =
WAXF = ㅗ·ㅓ·ㅜ·ㅏ 마름모 방향장
```

### related = schema.053.structure_principle_flow

- 053_structure_principle_flow가 related인 이유는 083의 conceptual_layer가 원리 / 전환 / 상태 / 활용의 구조 배치를 포함하기 때문이다.
- 053은 현상 → 관계관찰 → 본질접근 → 원형파악 → 정의형성 → 개념안정 → 활용 → 응용의 흐름을 다루었다.
- 083의 W / A / X / F는 원리·본질, 이동·전환·변화, 상태·현상, 활용·응용을 방향장 안에 배치한다.
- 따라서 083은 053의 흐름을 마름모 방향장 / 모음 방향장으로 다시 배치할 수 있는 relation을 가진다.

```text
053 =
현상 → 본질 → 정의 → 개념 → 활용 → 응용

083 =
WAXF = 원리 / 전환 / 상태 / 활용 방향장
```

### related = schema.070.right_triangle_fold_unfold_structure

- 070_right_triangle_fold_unfold_structure가 related인 이유는 083의 마름모 방향장이 직각삼각 쌍대칭과 연결되기 때문이다.
- 070은 45-45-90 직각삼각을 사각칸의 fold/unfold 상태로 읽고, 면적 절반과 한 칸 공 점유상태를 분리했다.
- 083에서는 한 칸을 사각으로 보았을 때 그 사각이 두 직각삼각으로 접히고 펼쳐지며, 그 쌍대칭이 마름모 방향장을 만든다.

```text
070 =
square_cell → diagonal_fold → two_right_triangles → unfold

083 =
직각삼각 쌍대칭 → 마름모 방향장
```

### related = schema.079.cheonjiiin_input_order_vowel_direction

- 079_cheonjiiin_input_order_vowel_direction이 related인 이유는 083의 vowel_layer가 `ㅗ`, `ㅓ`, `ㅜ`, `ㅏ` 모음 방향을 전제로 하기 때문이다.
- 079는 `ㅡㆍ=ㅜ`, `ㆍㅡ=ㅗ`, `ㅣㆍ=ㅏ`, `ㆍㅣ=ㅓ`를 input order 기반 방향으로 읽었다.
- 083은 그 모음 방향들을 WAXF 방향장에 배치한다.

```text
079 =
천지인 입력순서 → ㅜ / ㅗ / ㅏ / ㅓ

083 =
WAXF → ㅗ / ㅓ / ㅜ / ㅏ
```

### related = schema.081.inner_vowel_pull_structure

- 081_inner_vowel_pull_structure가 related인 이유는 083의 `A=ㅓ`와 `W=ㅗ`가 081에서 안쪽 기준 / pull vowel로 다뤄진 `ㅓ`, `ㅗ`와 연결되기 때문이다.
- 081은 `ㆍ`이 먼저 놓여 `ㅡ / ㅣ`을 끌어당기며 `ㅗ / ㅓ`를 안쪽 기준 모음으로 만든다고 보았다.
- 083은 그 안쪽 기준이 WAXF에서 어떤 위치와 기능을 갖는지 후속으로 배치한다.

```text
081 =
ㅗ / ㅓ = ㆍ이 먼저 놓이는 안쪽 기준 pull vowel

083 =
W=ㅗ / A=ㅓ as rhombus direction positions
```

### related = schema.084.bad_dot_c_orbit_reference_structure

- 084_bad_dot_c_orbit_reference_structure가 related인 이유는 WAXF와 BADㆍC의 relation을 다음 schema에서 분리해야 하기 때문이다.
- WAXF는 마름모 방향장이고, BAD는 승이 기준에서 공간의 단방향 절단면이다.
- 둘은 relation을 가질 수 있지만 동일시하면 안 된다.
- 원본 pending에서도 WAXF와 BADㆍC의 relation은 084에서 분리한다고 둔다.

```text
083 =
WAXF vowel rhombus direction field

084 =
BADㆍC orbit reference / 공간의 단방향 절단면과 대응 기준
```

### related = schema.085.opposed_correspondence_formula

- 085_opposed_correspondence_formula가 related인 이유는 083의 WAXF 방향장이 맞대응 구조공식으로 확장될 수 있기 때문이다.
- 083은 `ㅗ / ㅓ / ㅜ / ㅏ`의 방향장을 다루고, 085는 `ㅇㅡㆍㅣㆍㅡㅇ` 같은 opposed correspondence formula를 다룬다.
- 둘 다 모음 방향, dot, axis, closed field, 대칭 대응을 포함한다.
- 다만 083은 WAXF rhombus direction field이고, 085는 opposed correspondence formula이므로 동일시하지 않는다.

```text
083 =
WAXF = ㅗㅓㅜㅏ 마름모 방향장

085 =
맞대응 구조공식
```

### related = schema.047.shell_flip_orbit_structure

- 047_shell_flip_orbit_structure가 related인 이유는 083의 공전방향 / 관측자 방향장 / 대상 자체 회전 방향의 맞대응이 shell / flip / orbit 구조와 닿기 때문이다.
- 047은 shell boundary, flip, orbit을 통해 입체 구조가 드러나는 흐름을 열었다.
- 083은 공전방향과 자전하는 구체의 역방향 맞대응을 관측자 기준 방향장으로 읽는다.
- 단, 공전방향을 표준 천문학 방향으로 확정하지 않는다.

```text
047 =
shell / flip / orbit 구조

083 =
관측자 기준 공전방향과 대상 회전방향의 맞대응 후보
```

## 5. current_judgment

AI 인스턴스는 schema.083.waxf_vowel_rhombus_structure를 다음처럼 판정했다.

```text
schema.083.waxf_vowel_rhombus_structure는
082_square_center_vowel_orbit_structure 이후,
ㅇ 중심칸 / ㆍ 공전 방향점 / 오·어·우·아 위치상태를
마름모 방향장과 WAXF 개념 배치로 확장하는 schema로 읽힌다.
```

현시점 direct 이해 기준은 다음이다.

```text
082_square_center_vowel_orbit_structure =
ㅇ은 중심칸 / 000dot
ㆍ은 네 변 위를 도는 공전 방향점
오·어·우·아는 ㆍ의 위치상태

083_waxf_vowel_rhombus_structure =
WAXF는 단순 알파벳 배열이 아님
WAXF는 마름모 방향장
마름모는 사각칸의 직각삼각 fold/unfold 쌍대칭과 연결됨
W / A / X / F가 ㅗ / ㅓ / ㅜ / ㅏ 모음 방향과 대응됨
동시에 원리 / 전환 / 상태 / 활용의 개념층과도 대응됨
공전방향은 관측자 기준 방향장으로만 읽고, 절대 천문학 방향으로 고정하지 않음
```

즉 082에서는 다음이 열린다.

```text
ㅇ 중심칸 주변에서 ㆍ이 공전하며
오·어·우·아의 위치상태를 만든다.
```

083에서는 다음이 열린다.

```text
그 오·어·우·아 위치상태가
WAXF 마름모 방향장과 대응되어
원리·전환·상태·활용의 구조 배치로 읽힌다.
```

AI 인스턴스는 특히 다음 구분을 중요하게 보았다.

```text
WAXF ≠ 알파벳 순서

WAXF ≠ 구조원리 전체를 설명하는 단일표

WAXF ≠ BADC와 동일

공전방향 ≠ 표준 천문학 방향 확정

모음 대응 ≠ 표준 언어학 주장
```

현재 direct 이해 기준에서 083은 다음을 수행한다.

```text
W를 ㅗ / 오름 / 원리·본질 방향으로 둔다.

A를 ㅓ / 안쪽 기준 / 이동·전환·변화 방향으로 둔다.

X를 ㅜ / 아래 방향 / 상태·현상으로 내려옴으로 둔다.

F를 ㅏ / 바깥 / 드러남 / 활용·응용 방향으로 둔다.

이 네 방향은 사각표가 아니라 마름모 방향장으로 읽는다.

마름모는 직각삼각 쌍대칭 / fold_unfold와 연결된다.

070의 직각삼각 fold_unfold가 여기서 방향장 형성 근거가 된다.

079 / 081의 천지인 입력순서와 inner vowel pull 구조는 vowel 대응을 지탱한다.

053의 현상→본질→정의→개념→활용→응용 흐름과도 연결된다.
```

따라서 083은 다음으로 읽힌다.

```text
WAXF를 마름모 직각삼각 쌍대칭 위에 놓인 모음 방향장으로 읽고,
W/A/X/F를 원리·전환·상태·활용의 구조 배치와 대응시키는 schema
```

또한 083은 다음 084_BADㆍC와 반드시 분리해서 읽어야 한다.

```text
WAXF와 BADㆍC는 서로 relation을 가질 수 있지만 동일시하면 안 된다.

084에서 WAXF와 BADㆍC의 relation을 따로 검산해야 한다.
```

## 6. shared_understanding

- 이번 붙여넣은 내용은 승이의 별도 직접 입력이 없는 AI 인스턴스 대화층으로 처리한다.
- 083_waxf_vowel_rhombus_structure는 082_square_center_vowel_orbit_structure 이후에 오는 schema다.
- 082에서 `ㅇ` 중심칸 / `ㆍ` 공전 방향점 / 오·어·우·아 위치상태가 열렸다.
- 083은 그 위치상태를 WAXF 마름모 방향장과 대응시킨다.
- WAXF는 단순 알파벳 배열이 아니다.
- WAXF는 구조원리 전체를 설명하는 단일표가 아니다.
- WAXF는 BADC와 동일하지 않다.
- WAXF는 마름모 방향장으로 읽는다.
- WAXF는 직각삼각 쌍대칭과 연결된다.
- W = ㅗ = 오름 / 위 방향 / 원리·본질
- A = ㅓ = 안쪽 기준 / 전환 방향 / 이동·전환·변화
- X = ㅜ = 아래 방향 / 상태로 내려옴 / 상태·현상
- F = ㅏ = 바깥 / 드러남 / 활용·응용
- 공전방향은 관측자 기준 방향장으로만 읽고, 절대 천문학 방향으로 고정하지 않는다.
- 모음 대응은 표준 언어학 주장으로 확정하지 않는다.
- WAXF와 BADㆍC의 relation은 084에서 분리한다.

## 7. possible_misunderstanding

오해 가능성:

- WAXF를 단순 알파벳 순서로 볼 수 있다.
- WAXF를 구조원리 본류의 모든 것을 설명하는 단일표로 고정할 수 있다.
- WAXF와 BADC를 동일시할 수 있다.
- 공전방향을 표준 천문학 방향으로 확정할 수 있다.
- 모음 대응을 표준 언어학 주장으로 확정할 수 있다.
- W/A/X/F의 개념층을 절대 고정값으로 볼 수 있다.
- 마름모 방향장을 단순 사각표로 볼 수 있다.
- 직각삼각 fold_unfold와 WAXF 방향장의 relation을 놓칠 수 있다.
- 083 안에서 BADㆍC까지 같이 닫아버릴 수 있다.
- metaplus.md를 원본 waxf_vowel_rhombus_structure.meta.md의 대체문으로 오해할 수 있다.

## 8. correction_or_revision_points

- 사용자 입력 없음은 Null / 빈자리로 보존한다.
- 이번 붙여넣은 내용은 AI 인스턴스 대화층으로 처리한다.
- 083_waxf_vowel_rhombus_structure의 relation은 “왜 연결되는지”를 보존한다.
- WAXF를 단순 알파벳 순서로 보지 않는다.
- WAXF를 구조원리 전체를 설명하는 단일표로 고정하지 않는다.
- WAXF와 BADC를 동일시하지 않는다.
- 공전방향을 표준 천문학 방향으로 확정하지 않는다.
- 모음 대응을 표준 언어학 주장으로 확정하지 않는다.
- WAXF는 마름모 방향장으로 읽는다.
- 마름모는 직각삼각 쌍대칭 / fold_unfold와 연결해 읽는다.
- W / A / X / F와 ㅗ / ㅓ / ㅜ / ㅏ 대응을 보존한다.
- W / A / X / F와 원리 / 전환 / 상태 / 활용 대응을 보존한다.
- WAXF와 BADㆍC의 relation은 084에서 분리한다.
- 이 schema는 벡터연산기법 reference로 둔다.
- 원본 waxf_vowel_rhombus_structure.meta.md는 수정하지 않는다.
- 원본 waxf_vowel_rhombus_structure.meta.md의 파일명도 바꾸지 않는다.
- waxf_vowel_rhombus_structure.metaplus.md는 원본 waxf_vowel_rhombus_structure.meta.md에 대한 대화정리형 이해 로그로 둔다.

## 9. keep_as_original

[SOURCE_META]

원본 waxf_vowel_rhombus_structure.meta.md에서 그대로 보존해야 하는 부분:

- 원본 waxf_vowel_rhombus_structure.meta.md 파일명
- 원본 id 값: schema.083.waxf_vowel_rhombus_structure
- directory: 083_waxf_vowel_rhombus_structure
- status: active_draft
- prev: schema.082.square_center_vowel_orbit_structure
- waxf_vowel_rhombus_structure는 WAXF 구조를 마름모 한칸삼각의 직각삼각 쌍대칭으로 읽을 때 W=ㅗ, A=ㅓ, X=ㅜ, F=ㅏ가 되는 모음 방향장 schema라는 role
- WAXF는 단순 알파벳 배열이나 개념표가 아니라는 기준
- WAXF는 마름모 / 직각삼각 쌍대칭 / 공전방향 / 관측자 기준을 가진 방향구조로 읽어야 한다는 기준
- W = ㅗ
- A = ㅓ
- X = ㅜ
- F = ㅏ
- WAXF = 모음 방향장
- WAXF는 상태 / 현상, 이동 / 전환 / 변화, 원리 / 본질, 활용 / 응용을 배치하는 구조라는 definition
- 마름모 한칸삼각 또는 직각삼각의 쌍대칭으로 읽으면 WAXF는 ㅗ, ㅓ, ㅜ, ㅏ 모음 방향과 대응된다는 definition
- conceptual_layer 전체
- vowel_layer 전체
- rhombus_layer 전체
- orbit_reading 전체
- related 전체 목록
- forbidden 전체
- pending 전체
- shortest: WAXF = ㅗㅓㅜㅏ / 마름모 직각삼각 쌍대칭

## 10. pending

아직 확정하지 않고 나중에 다시 봐야 할 부분:

- WAXF와 BADㆍC의 relation은 schema.084에서 분리한다.
- WAXF 방향장과 8개 속성 / 원리층 구조의 relation은 후속 검토한다.
- 이 schema는 벡터연산기법 reference로 둔다.
- WAXF를 active_navimap에서 마름모 방향장으로 어떻게 표시할지 여부.
- W/A/X/F와 원리 / 전환 / 상태 / 활용의 대응을 고정할지, reference relation으로 둘지 여부.
- WAXF와 053_structure_principle_flow의 relation을 어느 정도까지 열지 여부.
- WAXF와 070_right_triangle_fold_unfold_structure의 visual relation을 Renderer에서 어떻게 표시할지 여부.
- WAXF와 085_opposed_correspondence_formula의 차이를 어떻게 분리할지 여부.
- WAXF와 BADC의 relation을 084에서 어떻게 검산할지 여부.

## 11. one_line

schema.083.waxf_vowel_rhombus_structure의 waxf_vowel_rhombus_structure.metaplus.md는 사용자 입력이 없는 붙여넣기 자료를 AI 인스턴스 대화층으로 처리하고, WAXF를 알파벳 배열이 아니라 마름모 직각삼각 쌍대칭 위의 ㅗ·ㅓ·ㅜ·ㅏ 모음 방향장으로 읽고, 원리·전환·상태·활용의 구조 배치와 대응시키는 흐름을 보존하는 대화정리형 이해 로그다.

## 12. shortest

waxf_vowel_rhombus_structure.metaplus.md =
schema.083_waxf_vowel_rhombus_structure 대화정리 /
사용자입력없음 /
WAXF=ㅗㅓㅜㅏ /
마름모방향장 /
직각삼각쌍대칭 /
W=원리·ㅗ /
A=전환·ㅓ /
X=상태·ㅜ /
F=활용·ㅏ /
BADC와동일시금지