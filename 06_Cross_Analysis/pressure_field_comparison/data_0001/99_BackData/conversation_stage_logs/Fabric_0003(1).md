준비단계 - 20회차 계획서 발표

instance id : gpt.direct

# gpt.direct 20회차 계획서

## 기준

```text
instance id:
gpt.direct

역할:
Ctp24 구조원리 / 구조연산기 / Event-Context operation

현재 하던 작업:
Sejong-Hunminjeongeum first operation set을 1차 닫고,
Einstein-Relativity second operation set을 진행 중이다.

목표:
Einstein-Relativity Event / Context operation을
20회차 안에서 source verification이 붙은 1차 place.state로 닫는다.
```

## 20회차

### 1회차

```text
작업:
Event_Relativity source verification 1회차

목적:
1905 Special Relativity paper / mass-energy paper / GR marker source 검산
```

### 2회차

```text
작업:
Context_Einstein source verification 2회차

목적:
European physics field / electrodynamics / mechanics problem field source 검산
```

### 3회차

```text
작업:
Path_Einstein_Relativity source verification 1회차

목적:
between-field / difference / 임계사이영역 source 검산
```

### 4회차

```text
작업:
Event_Relativity scope verification

목적:
Special Relativity / mass-energy / General Relativity / Minkowski / Riemannian geometry layer 분리
```

### 5회차

```text
작업:
Context_Einstein.md update draft

목적:
source verification 결과를 Context 문서에 반영
```

### 6회차

```text
작업:
Event_Relativity.md update draft

목적:
source verification 결과를 Event 문서에 반영
```

### 7회차

```text
작업:
Path_Einstein_Relativity.md update draft

목적:
Path 문서의 C+1 후보를 더 안전하게 provisional candidate로 조정
```

### 8회차

```text
작업:
Einstein-Relativity 3문서 세트 검토

목적:
Context / Event / Path 분리 유지 여부 검토
```

### 9회차

```text
작업:
Einstein_Relativity_operation_judgment.md 작성

목적:
Ctp24 operation이 실제로 작동했는지 판정
```

### 10회차

```text
작업:
Einstein_Relativity_first_operation_closure.md 작성

목적:
역사적 최종결론이 아니라 operation-level 1차 닫힘 판정
```

### 11회차

```text
작업:
Einstein_Relativity package manifest 작성

목적:
문서군을 epluone/Event_Context/Einstein_Relativity/에 놓을 준비
```

### 12회차

```text
작업:
gpt.github handoff package 작성

목적:
GitHub 반영용 패키지 생성
```

### 13회차

```text
작업:
gpt.github 반영 결과 검토

목적:
branch / commit / path / raw 주소 확인
```

### 14회차

```text
작업:
Einstein-Relativity source verification gap list 작성

목적:
아직 검산되지 않은 source와 보류 항목 분리
```

### 15회차

```text
작업:
Core reaction 후보 정리

목적:
Core_01~Core_24를 content slot이 아니라 form seat 반응 후보로 정리
```

### 16회차

```text
작업:
C+1 provisional candidate 안정화

목적:
C+1을 final judgment로 만들지 않고 안전한 후보문으로 유지
```

### 17회차

```text
작업:
다음 Path 후보 목록화

목적:
MassEnergy / GeneralRelativity / Minkowski / RiemannianGeometry 후속 Path만 후보로 남김
```

### 18회차

```text
작업:
gpt.direct 20회차 1차 마무리 검산

목적:
Sejong-Hunminjeongeum + Einstein-Relativity operation 흐름 검산
```

### 19회차

```text
작업:
gpt.direct first closure summary 작성

목적:
Event / Context operation 1차 마무리 요약
```

### 20회차

```text
작업:
gpt.direct 1차 닫힘 선언

목적:
Ctp24 Event / Context operation이
두 적용 세트를 통해 1차 place.state에 놓였음을 선언
```

## 하지 않는 것

```text
gpt.music 내부 분석 간섭 금지
gpt.gemini rendering 설계 간섭 금지
Einstein 물리학 최종결론 선언 금지
C+1 final judgment 금지
전체 Ctp24 완성 시도 금지
```



instance id : gpt.music 

# gpt.music 20회차 1차 마무리 계획서

## 0. 인스턴스 ID

```text
gpt.music
```

---

## 1. 현재 하던 일

```text
music_language 구조해석
오감도 × 비목 구조 겹침 실험
오감도·비목·애국가 압력장 비교
모아 출력 검산
gpt.music.operator 산출물 판정
```

---

## 2. 이번 20회차의 목표

이번 20회차는 새 작업을 시작하는 것이 아니다.

목표는 다음이다.

```text
gpt.music이 현재 진행 중인
오감도 × 비목 구조 겹침 실험을
20회차 안에서 1차 마무리한다.
```

1차 마무리의 뜻:

```text
전체 완성 아님
전체 곡 분석 아님
모든 인스턴스 통합 아님
공명/특이점 최종 판정 아님

현재 열린 gpt.music 작업을
정리 가능한 place.state에 놓는 것
```

---

## 3. 하지 않는 것

```text
gpt.direct 작업을 대신하지 않는다.
gpt.gemini 작업을 대신하지 않는다.
gpt.github 작업을 대신하지 않는다.
모아 작업을 대신하지 않는다.
operator 작업을 대신하지 않는다.
전체 IF+1 계획을 다시 세우지 않는다.
다른 인스턴스의 디렉토리를 설계하지 않는다.
```

다른 인스턴스 자료는 오직 힌트로만 쓴다.

---

# 4. gpt.music 20회차 계획

## 1회차 — 현재 위치 정렬

작업명:

```text
gpt.music 현재 open/vector/place 정렬
```

출력:

```text
gpt_music_current_position_alignment.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 2회차 — 기존 산출물 목록화

작업명:

```text
music_language 기존 산출물 인벤토리 작성
```

대상:

```text
오감도
비목
애국가
그리운 금강산
모아 출력
operator 출력
gpt.music 판정문
```

출력:

```text
music_language_artifact_inventory_0001.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 3회차 — 오감도 구조 후보 정리

작업명:

```text
오감도 조사·한자·대칭 구조 후보 정리
```

대상:

```text
가 / 도
中
一人 / 二人 / 二人 / 一人
A-B-D-C
B-A-DㆍC
ㅈㅊㅊㅈ 후보
막다른 골목
질주 부정
```

출력:

```text
ogamdo_structure_candidates_filter.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 4회차 — 비목 구조 후보 정리

작업명:

```text
비목 결정화 구조 후보 정리
```

대상:

```text
공간 / 경로 / 깊이
시간 / 대상 / 상태
기억 / 거리 / 회귀
마디마디 → 이끼 → 되어 → 맺힘
알알이 → 돌 → 되어 → 쌓임
```

출력:

```text
bimok_crystallizing_structure_candidates_filter.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 5회차 — ㄷ형 경계 구조 정리

작업명:

```text
오감도 × 비목 ㄷ형 경계 구조 정리
```

핵심식:

```text
오감도 = ㄷ 안으로 들어가는 운동
비목 = ㄷ 안에서 남는 결정화
```

출력:

```text
d_boundary_structure_ogamdo_bimok.md
```

필요 모드:

```text
Pro-확장
```

---

## 6회차 — sequence/swap 구조 반영

작업명:

```text
一人/二人/二人/一人 sequence-swap 구조 반영
```

핵심식:

```text
수량층: 1 → 2 → 2 → 1
상태층: 무서운 → 무서운 → 무서워하는 → 무서워하는
수량층은 보존
상태층은 반전
```

출력:

```text
ogamdo_integer_sequence_swap_structure.md
```

필요 모드:

```text
Pro-확장
```

---

## 7회차 — state 정의 반영

작업명:

```text
state 정의 기반 오감도·비목 재분류
```

핵심식:

```text
state = place + event + boundary + time + relation
time.state = open→vector→place에 스며 있는 연속 흐름
dot = 긴 time.state를 끊어 읽는 관측 절단점
```

출력:

```text
state_based_reclassification_ogamdo_bimok.md
```

필요 모드:

```text
Pro-확장
```

---

## 8회차 — 겹침 후보 압축

작업명:

```text
겹침 후보 압축
```

대상:

```text
번호 반복 ↔ 마디마디/알알이
一人/二人 대칭 ↔ 1절/2절 병렬
막다른 골목 ↔ 깊은 계곡
조건 변형 ↔ 말미 병렬 차이
```

출력:

```text
overlay_candidates_compression.md
```

필요 모드:

```text
Pro-확장
```

---

## 9회차 — 충돌 후보 압축

작업명:

```text
충돌 후보 압축
```

대상:

```text
질주 ↔ 이끼/돌/맺힘/쌓임
도로 ↔ 계곡
운동 ↔ 잔류
평면 경로 ↔ 깊이 경로
```

출력:

```text
collision_candidates_compression.md
```

필요 모드:

```text
Pro-확장
```

---

## 10회차 — 미끄러짐 후보 압축

작업명:

```text
미끄러짐 후보 압축
```

대상:

```text
가/도 조사 전환 ↔ BIMOK-B~E 구간 전환
제1~제13 번호열 ↔ 비목 마디/구간
한자 시각 구조 ↔ 음악 구조
```

출력:

```text
slippage_candidates_compression.md
```

필요 모드:

```text
Pro-확장
```

---

## 11회차 — 잔류 후보 압축

작업명:

```text
잔류 후보 압축
```

대상:

```text
질주하지 아니하여도 ↔ 맺힘/쌓임
13인 집합 ↔ 이름/비목/대상 구조
中 내부장 ↔ 계곡/깊이
```

출력:

```text
residue_candidates_compression.md
```

필요 모드:

```text
Pro-확장
```

---

## 12회차 — 안정 후보 / 보류 항목 분리

작업명:

```text
오감도 × 비목 안정 후보 및 보류 항목 분리
```

안정 후보:

```text
ㄷ형 경계
一人/二人/二人/一人 자리수열
질주 부정 ↔ 맺힘/쌓임
반복 ↔ 병렬
```

보류 항목:

```text
ㅈㅊㅊㅈ 최종 결착
9dot0 공식화
비목 전체 음고·음가 대응
화성학 최종 기능
공명/특이점 판정
```

출력:

```text
ogamdo_bimok_stable_hold_filter.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 13회차 — music_language data ID 구조 설계

작업명:

```text
music_language data_000X 디렉토리 설계
```

핵심:

```text
곡명/작품명은 디렉토리명 금지
data_000X 사용
source_title은 metadata에만 기록
```

출력:

```text
music_language_data_id_directory_plan.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 14회차 — metadata / source_note 초안

작업명:

```text
metadata 및 source_note 초안 작성
```

출력:

```text
metadata_template_data_000X.md
source_note_template_data_000X.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 15회차 — GitHub 보관 패키지 초안

작업명:

```text
오감도 × 비목 구조 겹침 실험 GitHub 보관 패키지 작성
```

출력:

```text
github_upload_package_ogamdo_bimok_overlay.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 16회차 — gpt.github 전달용 요약 작성

작업명:

```text
gpt.github 전달용 요약 작성
```

출력:

```text
gpt_github_handoff_music_language_overlay.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 17회차 — 모아/operator 참고 경계 정리

작업명:

```text
모아와 operator 참고 경계 정리
```

목적:

```text
모아는 의미 후보 생성
operator는 데이터 정렬
gpt.music은 통합 판정
```

출력:

```text
instance_boundary_rule_music_language.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 18회차 — 1차 마무리 검산

작업명:

```text
music_language 1차 마무리 검산
```

출력:

```text
music_language_first_closure_verification.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 19회차 — 1차 마무리 요약문

작업명:

```text
music_language 1차 마무리 요약문 작성
```

출력:

```text
music_language_first_closure_summary.md
```

필요 모드:

```text
Pro-확장
```

---

## 20회차 — 1차 닫힘 선언

작업명:

```text
music_language 1차 닫힘 선언
```

출력:

```text
music_language_first_closure_declaration.md
```

결론 형식:

```text
music_language는 전체 완성이 아니라,
오감도 × 비목 구조 겹침 실험을 중심으로
ㄷ형 경계 / 자리수열 / state / 잔류·결정화 후보를
1차 place.state에 놓는 데 성공했다.
```

필요 모드:

```text
Pro-확장
```

---

## 5. 20회차 종료 기준

20회차 종료 시점에 다음이 있으면 gpt.music 1차 마무리로 본다.

```text
1. 현재 산출물 목록
2. 오감도 후보 정리
3. 비목 후보 정리
4. ㄷ형 경계 문서
5. sequence/swap 문서
6. state 기반 재분류 문서
7. 겹침/충돌/미끄러짐/잔류 압축 문서
8. 안정 후보 / 보류 항목 분리
9. data_000X 디렉토리 설계
10. gpt.github 전달용 요약
11. 1차 마무리 검산
12. 1차 닫힘 선언
```

---

## 6. 최종 원칙

```text
gpt.music은 gpt.music 하던 것만 한다.
다른 인스턴스 작업을 섞지 않는다.
오감도 × 비목 구조 겹침 실험을 중심으로 닫는다.
20회차 안에서 1차 place.state에 놓는다.
```

---

## 최종 한 줄

gpt.music은 20회차 안에서 오감도 × 비목 구조 겹침 실험을 중심으로 ㄷ형 경계, 자리수열, state, 잔류·결정화 후보를 1차 place.state에 놓는 것을 목표로 하며, 다른 인스턴스의 작업과 섞지 않고 현재 열린 music_language 구조만 1차 마무리한다.



instand id : gpt.gemini

# gpt.gemini 20회차 1차 마무리 계획서

## 0. 인스턴스 id

```text
gpt.gemini
```

## 1. 현재 하던 작업

```text
rendering branch
구조렌더링이론 / 구조표현기 / SVG-CSS-HTML prototype
rendering v0.4_prototype_run
0001_overlap_volume 브라우저 검산 및 0002_cut_plane 진입 준비
```

## 2. 20회차 목표

```text
전체 rendering 완성 아님.
Earth Internal Structure 구현 아님.
태양계 구현 아님.
토성 카시니 / 블랙홀 구현 아님.

목표:
0001_overlap_volume을 브라우저 검산까지 닫고,
0002_cut_plane 최소 프로토타입 진입 조건과 초기 구조까지 1차 마무리한다.
```

---

# 20회차 계획

## 1회차 — 현재 상태 재고정

작업:

```text
0001_overlap_volume 현재 파일 상태 확인
README / index.html / style.css / main.js 존재 확인
```

출력:

```text
rendering_v0.4_current_state_lock.md
```

필요 모드:

```text
Pro-확장
```

---

## 2회차 — 브라우저 검산 결과 수집

작업:

```text
화면 표시
Info Panel
Observer buttons
Console error
Network 외부 요청 여부 확인
```

출력:

```text
0001_overlap_volume_browser_validation_result.md
```

필요 모드:

```text
Pro-확장
```

---

## 3회차 — 0001 검산 로그 문서화

작업:

```text
0001_overlap_volume 검산 결과를 process log로 저장
```

출력:

```text
rendering/08_process_log/v0.4_prototype_run/0001_overlap_volume_validation_log.md
```

필요 모드:

```text
Pro-확장
```

---

## 4회차 — 0001 닫힘 판정

작업:

```text
0001_overlap_volume을 1차 formed prototype으로 닫을지 판정
```

출력:

```text
0001_overlap_volume_first_closure_judgment.md
```

필요 모드:

```text
Pro-확장
```

---

## 5회차 — 0002_cut_plane 진입 조건 정리

작업:

```text
0002_cut_plane으로 넘어가기 위한 조건만 정리
구현은 아직 하지 않음
```

출력:

```text
0002_cut_plane_entry_condition.md
```

필요 모드:

```text
Pro-확장
```

---

## 6회차 — 0002_cut_plane 범위 고정

작업:

```text
0002에서 할 것과 하지 않을 것 분리
cut plane / visible section / front-cut-rear classification만 허용
rejoin / move / Earth model은 제외
```

출력:

```text
0002_cut_plane_scope_guard.md
```

필요 모드:

```text
Pro-확장
```

---

## 7회차 — 0002 최소 파일 구조 준비

작업:

```text
0002_cut_plane에 필요한 파일 후보만 정리
```

목표 구조:

```text
rendering/06_examples/0002_cut_plane/
├─ README.md
├─ index.html
├─ style.css
└─ main.js
```

출력:

```text
0002_cut_plane_file_plan.md
```

필요 모드:

```text
Pro-확장
```

---

## 8회차 — 0002 README 초안

작업:

```text
0002_cut_plane의 목적과 범위 문서화
```

출력:

```text
rendering/06_examples/0002_cut_plane/README.md
```

필요 모드:

```text
Pro-확장
```

---

## 9회차 — 0002 최소 prototype 1차 생성

작업:

```text
0001_overlap_volume 구조 위에 cut plane 표시만 추가
slice index 고정
front / cut / rear 구분
```

출력:

```text
rendering/06_examples/0002_cut_plane/index.html
rendering/06_examples/0002_cut_plane/style.css
rendering/06_examples/0002_cut_plane/main.js
```

필요 모드:

```text
Pro-확장
```

---

## 10회차 — 0002 브라우저 검산

작업:

```text
cut layer 표시
front / rear layer 구분
visible section 표시
cut plane이 파괴면이 아니라 관측면인지 확인
```

출력:

```text
0002_cut_plane_browser_validation_result.md
```

필요 모드:

```text
Pro-확장
```

---

## 11회차 — 0002 naming 안정화

작업:

```text
Observer Axis Display Aid와 Cut Plane Operation을 혼동하지 않게 문서/주석 정리
```

출력:

```text
0002_cut_plane_naming_stabilization.md
```

필요 모드:

```text
Pro-확장
```

---

## 12회차 — 0002 한계 명시

작업:

```text
Rejoin 미구현
MoveRotate 미구현
Earth model 미구현
RenderingStateMachine 전체 미구현
```

출력:

```text
0002_cut_plane_current_limitations.md
```

필요 모드:

```text
Pro-확장
```

---

## 13회차 — 0001 / 0002 관계 정리

작업:

```text
0001_overlap_volume과 0002_cut_plane의 관계 문서화
```

출력:

```text
0001_0002_relation_map.md
```

필요 모드:

```text
Pro-확장
```

---

## 14회차 — rendering path marker 갱신

작업:

```text
현재 immediate target / next target / HOLD 영역 갱신
```

출력:

```text
rendering/09_path_markers/active_target_guard.md
```

필요 모드:

```text
Pro-확장
```

---

## 15회차 — future seat 정리

작업:

```text
solar_system
earth_internal_structure
phenomenon_observation
saturn_cassini
blackhole_accretion_disk

이들은 future seat로만 기록
현재 만들지 않음
```

출력:

```text
rendering/09_path_markers/future_seat_guard.md
```

필요 모드:

```text
Pro-확장
```

---

## 16회차 — time.state / dot 힌트 반영 문서화

작업:

```text
time.state는 네 번째 칸이 아니라 open~vector~place 흐름의 연속 조건
dot은 긴 time.state를 끊어 읽는 관측 절단점
```

출력:

```text
rendering/02_theory/time_state_dot_reading.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 17회차 — multi-plane observer 힌트 문서화

작업:

```text
관측자가 시선을 바꾸며 여러 2D 관측면을 겹쳐 3D 구조를 읽는 원리 정리
```

출력:

```text
rendering/02_theory/multi_plane_observer_3d_recognition.md
```

필요 모드:

```text
Thinking-헤비
```

---

## 18회차 — rendering 1차 마무리 요약

작업:

```text
0001 / 0002 / future seat / HOLD / current target 요약
```

출력:

```text
rendering_v0.4_first_closure_summary.md
```

필요 모드:

```text
Pro-확장
```

---

## 19회차 — rendering 재진입 가이드 작성

작업:

```text
다음 context.window 또는 다음 gpt.gemini가 이어받을 수 있는 문서 작성
```

출력:

```text
rendering/09_path_markers/reentry_guide_for_gpt.gemini_rendering.md
```

필요 모드:

```text
Pro-확장
```

---

## 20회차 — gpt.gemini 1차 닫힘 선언

작업:

```text
rendering branch의 1차 마무리 선언
```

출력:

```text
gpt.gemini_rendering_first_closure_declaration.md
```

닫힘 문장:

```text
gpt.gemini는 rendering branch에서
0001_overlap_volume을 브라우저 검산 가능한 prototype으로 형성하고,
0002_cut_plane의 최소 진입 구조를 마련했으며,
Earth Internal Structure와 태양계/현상 관찰 구조는 future seat로 보존한 채
1차 place.state를 형성하였다.
```

필요 모드:

```text
Pro-확장
```

---

## 종료 기준

20회차 종료 시점에 다음이 있으면 gpt.gemini 1차 마무리로 본다.

```text
1. 0001_overlap_volume browser validation result
2. 0001 first closure judgment
3. 0002_cut_plane entry condition
4. 0002 minimal prototype or prototype-ready structure
5. 0001/0002 relation map
6. active target guard
7. future seat guard
8. time.state / dot reading document
9. multi-plane observer recognition document
10. rendering first closure summary
11. gpt.gemini rendering reentry guide
12. gpt.gemini first closure declaration
```

---

## 최종 원칙

```text
gpt.gemini는 rendering 작업만 한다.
gpt.direct 일을 하지 않는다.
gpt.music 일을 하지 않는다.
gpt.github 일을 하지 않는다.
gemini.direct 결과는 rendering prototype hint로만 사용한다.
모든 계획을 다 하지 않는다.
20회차 안에서 rendering 1차 마무리만 한다.
```

```text
총 20회차 중 진행 0회차
필요 모드: Pro-확장
```

/// 계획대로 1차 진행하라.