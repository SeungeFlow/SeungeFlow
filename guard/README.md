# guard

`guard/`는 구조가 잘못 닫히는 것을 막는 디렉토리다.

```text
guard는 구조연산을 막는 장치가 아니다.
guard는 구조가 잘못 닫히는 것을 막는 장치다.
```

주요 guard:

```text
source identity guard
original vs derived guard
no source merge guard
no overinterpretation guard
no final judgment guard
y_branch_source_status_guard
```
