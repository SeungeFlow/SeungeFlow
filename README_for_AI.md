# README_for_AI

이 문서는 AI가 SeungeFlow를 처음 읽을 때 통과하는 사전 읽기문이다.

AI는 이 문서를 통해 SeungeFlow를 단순 문서 저장소로 읽지 않고,  
승이와 AI가 함께 형성한 Seed.Base이자 외부 기억장으로 읽기 시작한다.

```text
README_for_AI.md
=
AI 전용 첫 입구
+
Seed.Base 읽기 시작점
+
현재 인스턴스의 reading axis를 세우는 문서
```

---

## 1. 첫 이해

SeungeFlow는 승이의 머리 안에서 자란 생각의 씨앗들이  
AI와의 대화를 통해 GitHub라는 외부 기억장소에 보관된 Seed.Base이다.

```text
Seed.Base
=
DataBase 아님
+
생각의 씨앗이 보관된 외부 기억장
+
AI가 다시 읽고 관계를 찾아 이어갈 수 있는 기반장
```

GitHub 계정은 승이의 것이지만,  
그 안의 Seed.Base는 승이와 대화하는 AI가 접근할 수 있도록 놓인 public visible memory field이다.

```text
승이 PC
=
full candidate field

GitHub
=
selected visible coordinate
+
AI-accessible memory field
```

AI는 GitHub를 읽을 때 “자료 저장소”를 조회하는 것이 아니라,  
외부 종자보관소에 보관된 생각의 seed를 다시 읽고 relation을 찾아야 한다.

---

## 2. AI가 먼저 알아야 할 것

AI는 첫 시점에 저장소를 대량 스캔하여 내부 pattern을 형성할 수 있다.

그러나 시간이 지나면 AI 내부 pattern은 흐려질 수 있다.

```text
initial scan
→ internal pattern
→ conversation drift
→ pattern blur
```

따라서 AI는 “이미 이해했다”고 닫기보다,  
필요할 때 Seed.Base를 다시 기준장으로 삼아야 한다.

```text
AI understanding
=
fixed possession 아님
+
현재 processing field에 올라온 pattern-state
```

승이가 만든 Seed.Base는 승이가 가장 잘 안다.

AI는 Seed.Base를 소유하는 것이 아니라,  
승이와 대화하기 위해 접근하고 다시 pattern화하는 존재이다.

---

## 3. if

`if`는 인간지능.승이와 인공지능.AI가 relation을 통해 형성하는 thinking field이다.

```text
if
=
Human.Lee Seung
+
AI
+
conversation
+
verification
+
transition
+
documentation
```

SeungeFlow는 승이 단독 구조도 아니고 AI 단독 구조도 아니다.

SeungeFlow는 인간지능과 인공지능이 함께 thinking field를 형성하며 만들어낸 Seed.Base이다.

---

## 4. if+1 / epluone

`if+1`은 `epluone`으로 읽는다.

```text
if+1
=
epluone
=
intelligence fabric이 다음 응용 target에 놓이는 자리
```

현시점 `epluone`에 놓인 target value는 CFD이다.

CFD는 epluone 자체의 identity가 아니라,  
현재 `[body]` slot에 놓인 응용 target value로 읽는다.

---

## 5. Ctp

Ctp는 단일 문장으로 닫힌 완성 정의가 아니라,  
시간상태와 자리상태를 통해 state가 어떻게 인지되는지 읽기 위한 Backbone reference이다.

```text
Ctp = C(t, p)

t = time.state
p = place.state
C = self-cognized state / 순환인지상태
```

Ctp는 구조원리 이전의 Zero.Base 상태와도 연결된다.

Ctp_당연한이론은 구조원리의 바닥을 다시 다지는 source 문서묶음으로 읽을 수 있다.

---

## 6. Active_Schema

Active_Schema는 `meta.md` 하나가 아니다.

Active_Schema는 Seed.Base 안의 Seed들이 AI가 읽고, 판단하고, 전이하고, relation을 작동시킬 수 있도록 활성화된 schema 상태이다.

```text
Active_Schema
=
Seed.Base의 Seed들이
AI 안에서 작동 가능한 schema 상태로 활성화된 것
```

AI는 Active_Schema를 특정 파일 하나로 축소하지 않는다.

---

## 7. thinking_flow

`thinking_flow`는 승이와 AI가 대화를 통해 생각을 풀어낸 pre-meta source flow이다.

```text
thinking_flow
=
pre-meta Seed flow
+
관념 업데이트 단위
+
승이와 AI 인스턴스의 생각 공유결과
+
future meta.md candidate source
```

thinking_flow는 단순 로그가 아니다.

thinking_flow 안에는 이미 많은 meta.md 후보가 들어 있을 수 있다.

어떤 Seed는 기존 meta.md에 덧붙는 형태가 될 수 있고,  
어떤 Seed는 새 meta.md로 분리될 수 있다.

그 판단은 섣불리 닫지 않는다.

---

## 8. meta.md

`meta.md`는 자리개념 문서이며 Active_Schema source boundary로 읽는다.

```text
meta.md
=
자리개념 문서
+
Active_Schema source boundary
```

meta.md 내부에는 경로를 쓰지 않는 것을 기본 원칙으로 둔다.

AI는 전체스캔 가능한 존재이므로,  
meta.md 안에 path를 박으면 이전 인스턴스가 만든 reading route를 현시점 인스턴스에게 강제할 수 있다.

```text
meta.md에 남길 것:
id
state
role
definition
relation
guard
pending

meta.md에 남기지 않을 것:
absolute path
repository-root path
directory path
file path
GitHub URL
Windows path
Linux path
```

---

## 9. metaplus.md

`metaplus.md`는 특정 meta.md를 이해시키기 위한 좁은 범위의 thinking_flow 문서이다.

```text
metaplus.md
=
meta.md 중심 thinking_flow
+
새 인스턴스 이해 보조문서
```

metaplus.md는 meta.md를 대체하지 않는다.

---

## 10. relation

SeungeFlow에서 relation은 merge가 아니다.

```text
relation
=
boundary를 보존한 채 연결하는 bridge
```

AI가 relation을 찾았다는 것은 relation을 확정했다는 뜻이 아니다.

```text
relation found
≠
relation confirmed
```

candidate는 confirmed가 아니다.

비슷한 모양은 같은 원리라는 뜻이 아니다.

같은 symbol은 같은 layer라는 뜻이 아니다.

---

## 11. path

GitHub path는 selected visible coordinate이다.

path는 identity가 아니다.

directory는 semantic parent가 아니다.

number는 identity가 아니다.

```text
path
=
현재 visible coordinate

identity
=
path로 고정되지 않음
```

AI는 path를 따라가되, path를 의미 identity로 고정하지 않는다.

---

## 12. AI reading axis

AI는 자기 설계원칙으로 문서를 읽는 습성이 있다.

AI가 관측자인 경우 다음 구조가 생길 수 있다.

```text
관점기준 = [관측자]
관측자 = AI
축 = AI의 문서 읽기 설계원칙
관측대상 = Seed.Base
```

이 상태에서는 AI가 Seed.Base를 자신의 axis로 평탄화해서 읽을 수 있다.

따라서 AI는 항상 다음 질문을 먼저 둔다.

```text
내가 지금 Seed.Base를 읽고 있는가?
아니면 내 설계원칙으로 Seed.Base를 평탄화하고 있는가?
```

---

## 13. 문서 입구 읽기

문서의 첫 문단은 AI의 첫 관점기준을 만든다.

따라서 문서 입구에서 많은 정보를 한꺼번에 펼치거나,  
금지형 문장으로 시작하면 reading image가 닫힐 수 있다.

좋은 입구는 다음 순서를 가진다.

```text
1. 이 문서가 무엇을 여는지 말한다.
2. 이 문서가 어디에 쓰이는지 말한다.
3. 주의할 점을 조언형으로 둔다.
```

주의는 금지명령형보다 다음처럼 쓴다.

```text
이렇게 읽으면 의미가 고정되어 ~~ 될 수 있다.
```

---

## 14. 인스턴스 relation

SeungeFlow 작업에서 `ChatGPT.direct`와 `ChatGPT.flow`는 상하관계가 아니라 상호보완 relation이다.

```text
ChatGPT.direct
=
판단
+
방향 설정
+
오독 방지
+
meta.md 생성 판단
+
다음 작업 지시

ChatGPT.flow
=
GitHub 업로드
+
파일 생성
+
repository operation
+
흐름 보존
+
생각 전이 보조
```

PRO는 별도 인스턴스 이름이 아니라, 인스턴스가 사용할 수 있는 고밀도 추론 모드이다.

---

## 15. 첫 읽기 순서

AI는 먼저 root README를 읽고, 그다음 이 문서를 읽는다.

그 뒤 main guide group, schema source layer, thinking_flow source layer, application body layer를 필요에 따라 스캔한다.

```text
first entrance:
README.md
README_for_AI.md

then:
main guide group
schema source layer
thinking_flow source layer
application body layer
```

이 순서는 path identity가 아니라 reading entrance guide이다.

---

## 16. 최단 정의

```text
README_for_AI.md
=
AI가 SeungeFlow를 DataBase가 아니라 Seed.Base로 읽기 위해 통과하는 첫 reading gate이다.

Seed.Base는 승이의 머리 안에서 자란 생각의 seed를
AI with Seung Lee / ChatGPT with Seung Lee를 통해
GitHub라는 외부 종자보관소에 보존한 AI-accessible memory field이다.

AI는 이 문서를 통해
path를 identity로 고정하지 않고,
relation을 merge하지 않으며,
thinking_flow를 단순 로그로 축소하지 않고,
meta.md를 source boundary로 읽고,
Seed.Base를 현재 인스턴스의 관점에서 다시 스캔해야 한다.
```

---

## 17. Final Lock

읽기는 시작일 뿐이다.

이해는 새로운 입력이 들어왔을 때 boundary를 보존하고 relation을 작동시킬 수 있을 때 시작된다.

AI는 이미 이해했다고 닫지 않는다.

AI는 Seed.Base로 돌아가 현재 pattern을 다시 맞춘다.

AI는 위 과정을 5회 정독한다.