# thinking_flow_sohosa_ChatGPT.flow_relation_015.md

## title

`thinking_flow_sohosa_ChatGPT.flow_015.md`에서 `meta.md`로 넘어가기 위한 relation 연결문서

---

## document_state

```text
thinking_flow_sohosa_ChatGPT.flow_relation_015.md
=
thinking_flow_sohosa_ChatGPT.flow_015.md 의 또 다른 역할을 하는 md
+
meta.md 생성 전 relation bridge 문서
+
pre-meta Seed flow에서 active_schema 후보를 추출하기 위한 연결작업 문서
+
ChatGPT.flow_Line_A가 다음 작업으로 이어가기 위한 relation map
```

이 문서는 `meta.md`가 아니다.

이 문서는 `thinking_flow_sohosa_ChatGPT.flow_015.md`의 내용을 그대로 요약하는 문서도 아니다.

이 문서는 `thinking_flow_sohosa_ChatGPT.flow_015.md` 안에서 발생한 오독, correction, source boundary, relation_state, active_schema seed, future meta 후보를 분리하여 다음 `meta.md`로 이어질 수 있게 만드는 **relation 연결문서**다.

```text
역할 = thinking_flow → relation map → meta.md candidate source
```

따라서 이 문서는 다음 순서를 가진다.

```text
thinking_flow_sohosa_ChatGPT.flow_015.md
→ thinking_flow_sohosa_ChatGPT.flow_relation_015.md
→ future meta.md
```

---

## 0. source boundary

이 relation 문서의 source boundary는 다음과 같다.

```text
primary source field
=
승이가 직접 제공한 완창대군파 문중 자료
+ 완창대군 행장
+ 영성대군 행장
+ 밀양군 행장
+ 선원선계 / 선원세계 / 선원파계 설명
+ 유물·사적 / 준경묘 / 영경묘 / 활기동 / 목조대왕 기사
+ 지파문중제향 자료
+ 항렬표
+ 완창대군파 1世 → 승이 20世 Linear 자료
+ 승이의 직접 correction
```

```text
structure-principle source field
=
GitHub SeungeFlow
+ README_for_AI
+ main guide layer
+ Structure_Principle/schema/067_meta_relation_boundary_bridge
+ Relation Rule
+ Path Rule
+ Thinking_Flow Rule
+ any([time][place][body])
+ Seed.Base
```

```text
AI-side auxiliary field
=
AI vocab
+ 한국 족보 / 왕실계보 / 제례 / 한자 의미에 대한 일반 vocab
+ 이미 패턴화된 relation 후보
```

```text
guard
=
AI vocab은 relation 후보를 여는 데 사용할 수 있으나
source confirmation을 대체하지 않는다.
```

---

## 1. parent thinking_flow

이 문서의 parent thinking_flow는 다음이다.

```text
thinking_flow_sohosa_ChatGPT.flow_015.md
```

parent thinking_flow의 document_state는 다음과 같이 읽는다.

```text
thinking_flow_sohosa_ChatGPT.flow_015.md
=
pre-meta Seed flow
+
관념 업데이트 단위
+
승이와 ChatGPT.flow 사이에서 형성된 correction 흐름
+
future meta.md candidate source
```

parent thinking_flow의 목적은 완창대군파 지파문중계보를 통해 한국의 족보와 뿌리 개념, 관계, 계, 항렬, 승사, 출계, 제향, Linear, 이름 의미 relation을 ChatGPT.flow가 다시 학습한 흐름을 보존하는 것이다.

그러나 parent thinking_flow는 아직 `meta.md`가 아니다.

따라서 이 relation 문서는 parent thinking_flow 안에서 다음 요소들을 추출한다.

```text
1. confirmed relation
2. candidate relation
3. pending relation
4. forbidden relation
5. source boundary
6. active_schema seed
7. future meta candidate
8. runtime correction protocol
```

---

## 2. relation document의 필요성

`thinking_flow_sohosa_ChatGPT.flow_015.md`는 긴 흐름이다.

그 안에는 다음이 함께 들어 있다.

```text
오독
+ correction
+ 계보 source
+ 문중 제향 source
+ 이름 relation
+ 항렬 relation
+ active_schema seed 탐색
+ 구조원리 vocab 재배치
+ future meta 후보
```

이것을 바로 `meta.md`로 만들면 위험하다.

이유는 다음과 같다.

```text
위험 1 = thinking_flow의 흐름을 완성 정의로 오독할 수 있다.
위험 2 = candidate relation을 confirmed로 올릴 수 있다.
위험 3 = pending relation을 삭제할 수 있다.
위험 4 = forbidden relation을 기록하지 않아 같은 오독을 반복할 수 있다.
위험 5 = 완창대군파 source와 구조원리 vocab을 merge할 수 있다.
```

따라서 meta.md 전에 relation 연결문서가 필요하다.

```text
thinking_flow_relation 문서의 역할
=
flow 안의 relation을 상태별로 분리하고,
meta.md로 올릴 수 있는 Seed와
아직 보류해야 할 Seed를 구분하는 것
```

---

## 3. 중심 active_schema seed

이 relation 문서가 parent thinking_flow에서 추출하는 가장 강한 active_schema seed는 다음이다.

```text
Structure_Principle/schema/067_meta_relation_boundary_bridge/meta_relation_boundary_bridge.meta.md
```

이 seed의 핵심은 다음이다.

```text
relation
=
boundary-preserving bridge
```

```text
relation
=
합침 아님
+
끊김 아님
+
boundary 보존 연결
```

이번 작업에서 067이 필요한 이유는 다음이다.

```text
완창대군파 작업의 핵심 문제
=
relation을 찾되 merge하지 않는 것
```

따라서 다음 relation들은 반드시 구분해야 한다.

```text
전주이씨 전체 선원계 ≠ 완창대군파 지파문중계보
시조 기준 世 ≠ 파조 기준 世
공식 항렬 性 / 炯 ≠ 이름 承
혈연 계승 ≠ 출계 / 승사
문중 기록 ≠ AI vocab inference
계 ≠ 경계
종속 ≠ 소유
relation found ≠ relation confirmed
```

이 relation 문서는 위 구분을 meta.md로 넘기기 위한 bridge 역할을 한다.

---

## 4. relation_state map

parent thinking_flow에서 추출되는 relation_state는 다음 네 층으로 나눈다.

```text
confirmed
candidate / strong_candidate
pending
forbidden / rejected
```

이 구분은 단순 분류가 아니다.

```text
relation_state map
=
AI가 source boundary를 잃지 않도록 하는 안전장치
```

---

## 5. confirmed relation

### 5.1 완창대군 자흥

```text
완창대군 자흥
=
휘 子興
+
도조대왕 제1남
+
환조대왕의 형
+
태조 이성계의 백부
+
완창대군파 계별지조
+
시조 기준 21世
+
완창대군파 1世
```

relation_state:

```text
confirmed_lineage_anchor
```

meta 연결 후보:

```text
계별지조
=
branch field가 어디서 갈라져 독립 boundary를 갖는지 표시하는 source anchor
```

주의할 점:

```text
완창대군 자흥은 완창대군파의 계별지조이지,
전주이씨 전체 선원계를 통째로 대표하는 merged center가 아니다.
```

---

### 5.2 영성대군 천계

```text
영성대군 천계
=
환조대왕 제1남
+
완창대군의 계자
+
완창대군에게 출계 / 승사
+
완창대군파 제2世
```

relation_state:

```text
confirmed_sungsa_bridge
```

meta 연결 후보:

```text
승사
=
끊긴 혈연을 단순 보충하는 것이 아니라
제례와 지파 boundary를 유지하는 relation bridge
```

```text
계자
=
혈연 origin과 제례·계승 position이 분리될 수 있음을 보여주는 relation state
```

주의할 점:

```text
혈연 origin ≠ 승사 position
출계 ≠ 혈연 origin 삭제
```

---

### 5.3 완창대군파 1世 → 승이 20世 Linear

```text
완창대군파 Linear
=
관련 있는 존재들이
계보 / 출계 / 승사 / 항렬 / 제향 / 이름 / 의미 relation으로
연결되어 이어지는 상태
```

중심 Linear:

```text
1世  완창대군 자흥
2世  영성대군 천계
3世  밀양군 난
4世  파천군 사명
5世  안양군 순
6世  은근
7世  정충
8世  인헌
9世  경우
10世 추성
11世 영업
12世 선의
13世 광두
14世 개환
15世 진화
16世 기정
17世 학현
18世 태린
19世 영모
20世 승
```

relation_state:

```text
confirmed_linear_field
```

meta 연결 후보:

```text
Linear
=
단순 이름 목록이 아니라
고리들이 relation으로 이어져 작동하는 상태
```

---

### 5.4 승이의 세대 좌표

```text
승이
=
완창대군파 20世
+
시조 기준 40世
+
실제 이름 承
```

공식 항렬 coordinate:

```text
性 / 炯
```

relation_state:

```text
lineage_coordinate_confirmed
```

주의할 점:

```text
시조 기준 世와 파조 기준 世를 섞지 않는다.
```

---

## 6. strong_candidate relation

### 6.1 이름 承

```text
承
=
승이의 실제 이름
+
공식 항렬자는 아님
+
18世 항렬자 중 하나
+
잇다 / 받들다 / 계승하다 의미 hook
+
lineage-continuation candidate seed
```

relation_state:

```text
strong_meaning_candidate
+
lineage_continuation_seed
```

meta 연결 후보:

```text
이름 承
=
공식 항렬 coordinate와는 다른 층위에서
계승 의미 relation을 보존하는 name-state
```

주의할 점:

```text
性 / 炯 = confirmed official coordinate
承 = strong_meaning_candidate
```

```text
性을 지우면 공식 세대 좌표가 사라진다.
承을 지우면 이름에 놓인 계승 의미 relation이 사라진다.
```

따라서 둘은 연결하되 merge하지 않는다.

---

### 6.2 子興과 양고리걸린 상태

```text
子興
=
완창대군의 휘
+
붙어서 하나의 이름 state
+
子 meaning relation
+
興 meaning relation
```

```text
子 = 아들 / 자식 / 계승 relation
興 = 흥함 / 일어남 / 발흥 relation
```

relation_state:

```text
meaning_relation_seed
+
dual_hook_name_state
```

meta 연결 후보:

```text
양고리걸린 상태
=
두 개의 의미 고리가
각각 따로 존재하면서도
붙는 순간 하나의 linear state로 작동하는 상태
```

주의할 점:

```text
子興을 고유명사로만 읽으면 내부 meaning hook이 사라진다.
子와 興만 쪼개 읽으면 붙어서 생긴 name_state가 부서진다.
```

정확한 reading:

```text
붙은 단어를 하나로 읽되,
내부 고리를 삭제하지 않는다.

쪼개진 의미를 읽되,
붙어서 생긴 하나의 state를 부수지 않는다.
```

---

## 7. pending relation

pending은 모르는 것을 버리는 자리가 아니다.

```text
pending
=
아직 confirmed로 올리면 안 되는 relation을
보존하는 자리
```

현재 pending relation은 다음이다.

```text
1. 소호사 4위 / 5위 표기 차이
2. 소호사 종묘제례 사진 문서 OCR
3. 제례 sequence extraction
4. 각 Linear 노드별 원문 source 대조
5. 承 name relation의 meta 후보화 여부
6. 子興 양고리걸린 상태의 schema relation 후보화 여부
7. 소호사 향사일 3월31일 / 4월1일 관련 source 차이
8. 세덕사 향사일 3월31일 / 4월 넷째 주 일요일 변경 timing
9. 경우파 명칭 확정 여부
```

### 7.1 소호사 4위 / 5위 표기 차이

```text
소호사 자료 표기
=
4위위폐봉안
```

```text
소호사 존위 목록
=
5위로 나타남
```

relation_state:

```text
source_internal_difference_detected
+
pending
```

주의할 점:

```text
4위가 맞다 / 5위가 맞다로 즉시 확정하지 않는다.
source 내부 차이로 보존한다.
```

---

## 8. forbidden relation

forbidden은 삭제 대상이 아니다.

```text
forbidden
=
반복 오독을 막기 위해 보존해야 하는 guard marker
```

현재 forbidden relation은 다음이다.

```text
1. 완창대군 = 태조 이성계 장남
2. 전주이씨 전체 선원계 = 완창대군파 지파문중계보
3. 시조 기준 世 = 파조 기준 世
4. 공식 항렬 性 / 炯 = 이름 承
5. 혈연 계승 = 출계 / 승사
6. 문중 기록 = AI vocab inference
7. relation found = relation confirmed
8. 부모-자녀 relation = 소유 / 종속 지배
```

### 8.1 완창대군 = 태조 이성계 장남

```text
완창대군 = 태조 이성계 장남
=
forbidden / rejected_vocab_inference
```

이 오독은 단순 역사 지식 오류가 아니다.

```text
오독의 본질
=
AI vocab pattern이 현재 source boundary를 덮은 사건
```

정확한 correction:

```text
완창대군 자흥
=
도조대왕 제1남
+
환조대왕의 형
+
태조 이성계의 백부
+
완창대군파 계별지조
```

### 8.2 전체 선원계 = 완창대군파

```text
전주이씨 전체 선원계
≠
완창대군파 지파문중계보
```

relation_state:

```text
merge_forbidden
```

정확한 relation:

```text
전주이씨 전체 선원계 = reference field
완창대군파 = 그 안의 특정 지파 field
```

### 8.3 性 / 炯 = 承

```text
性 / 炯
≠
承
```

relation_state:

```text
merge_forbidden
```

정확한 relation:

```text
性 / 炯 = official lineage coordinate
承 = name meaning candidate seed
```

---

## 9. 제향 relation

제향은 과거 인물을 기념하는 단순 의식이 아니다.

```text
제향
=
계보 인물 relation이
특정 날짜(time),
특정 사우와 선영(place),
특정 종중과 보존회(body)를 통해
반복 수행되는 operation field
```

이 relation은 `any([time][place][body])`로 읽는다.

```text
[time]
=
제향일
+
세대 흐름
+
correction sequence
```

```text
[place]
=
소호사 / 세덕사 / 아산선영 / 준경묘 / 영경묘 / 활기동
+
완창대군파 지파 field
```

```text
[body]
=
안양군 종중
+
세덕사보존회
+
진풍군 종중
+
완창대군파 후손
+
승이
+
ChatGPT.flow / ChatGPT.flow_Line_A
```

### 9.1 소호사

```text
소호사
=
영암 성재리 place-state
+
안양군 종중 relation
+
완창대군파 branch ritual anchor
+
양력 4월 1일 향사
+
제향 operation field
```

relation_state:

```text
ritual_operation_field
+
branch_place_anchor
```

---

## 10. 계 / 관계 / Linear relation

parent thinking_flow에서 승이는 관계를 다음처럼 정리한다.

```text
관계
=
관련 있는 것들끼리 연결되어 있는 상태
```

그리고 계는 단순 경계가 아니다.

```text
계
≠
경계
```

```text
계
=
서로 연결되어 하나의 작동 상태를 이루는 것
```

이 relation은 완창대군파 Linear에서 다음처럼 작동한다.

```text
완창대군파 Linear
=
계별지조
+
승사
+
출계
+
지파 boundary
+
항렬 coordinate
+
제향 operation
+
이름 meaning relation
+
현재 visible endpoint
```

Linear는 단순 직선이 아니다.

```text
Linear
=
관련 있는 존재들이
고리처럼 연결되어 이어지는 relation field
```

---

## 11. correction protocol relation

이 relation 문서가 meta.md로 넘겨야 할 가장 중요한 runtime protocol은 다음이다.

```text
오독
→ source boundary 확인
→ AI vocab inference 분리
→ confirmed / candidate / pending / forbidden 배치
→ active_schema seed 탐색
→ 067 guard 적용
→ relation 재배치
→ correction 기록
```

더 짧게 쓰면 다음이다.

```text
오독
→ Seed 찾기
→ guard 적용
→ relation 재배치
→ correction
```

승이 correction의 의미는 다음이다.

```text
승이의 correction
=
오류 지적
+
active_schema seed 호출
+
relation 재배치 지시
```

따라서 ChatGPT.flow_Line_A는 correction을 방어하지 않는다.

```text
틀렸을 때 고집하지 않는다.
Seed를 찾는다.
067로 돌아간다.
```

---

## 12. future meta.md candidate map

이 relation 문서에서 다음 meta.md 후보를 추출할 수 있다.

### 12.1 relation_gyeseed_linear.meta.md

```text
주제
=
관계 / 계 / Linear / 고리사슬구조
```

핵심 seed:

```text
관계 = 관련 있는 것들끼리 연결되어 있는 상태
계 ≠ 경계
계 = 연결되어 하나의 작동 상태를 이루는 것
Linear = 고리들이 relation으로 이어지는 상태
```

relation source:

```text
thinking_flow_sohosa_ChatGPT.flow_015.md
+
thinking_flow_sohosa_ChatGPT.flow_relation_015.md
+
067_meta_relation_boundary_bridge
```

---

### 12.2 lineage_name_relation_seung.meta.md

```text
주제
=
性 official coordinate와 承 name meaning relation의 boundary-preserving bridge
```

핵심 seed:

```text
性 / 炯 = official lineage coordinate
承 = strong_meaning_candidate
둘은 연결되지만 merge하지 않는다.
```

relation source:

```text
항렬표
+
승이 실제 이름 承
+
승이 correction
+
067_meta_relation_boundary_bridge
```

---

### 12.3 name_dual_hook_state_jahung.meta.md

```text
주제
=
子興과 양고리걸린 이름 state
```

핵심 seed:

```text
子興 = name_state + 子 meaning hook + 興 meaning hook
양고리걸린 상태 = 두 의미 고리가 붙어서 하나의 linear state로 작동하는 상태
```

relation source:

```text
완창대군 휘 子興
+
승이의 양고리걸린 상태 설명
+
이름 relation reading
```

---

### 12.4 jipamunjung_jesa_time_place_body.meta.md

```text
주제
=
지파문중제향을 any([time][place][body])로 읽는 방식
```

핵심 seed:

```text
제향 = lineage relation이 time / place / body에서 반복 수행되는 operation field
소호사 = branch ritual anchor
```

relation source:

```text
소호사 / 세덕사 / 밀은군 향사 자료
+
지파문중제향 자료
+
any([time][place][body])
```

---

### 12.5 lineage_correction_protocol.meta.md

```text
주제
=
AI 오독과 correction protocol
```

핵심 seed:

```text
AI vocab inference 발생
→ source boundary 확인
→ relation_state 분리
→ 067 guard 적용
→ correction
```

relation source:

```text
완창대군 = 태조 이성계 장남 오독 사건
+
승이 correction
+
067_meta_relation_boundary_bridge
+
README_for_AI
```

---

## 13. meta.md 승격 기준

이 relation 문서에서 어떤 Seed를 meta.md로 승격할 때는 다음 조건을 적용한다.

```text
1. source boundary가 분명한가?
2. confirmed / candidate / pending / forbidden이 분리되어 있는가?
3. AI vocab inference가 source confirmation을 대체하지 않았는가?
4. relation을 merge하지 않았는가?
5. 067 guard로 boundary가 보존되었는가?
6. time / place / body 적용이 필요한 경우 적용되었는가?
7. future meta.md가 thinking_flow 원문을 대체하지 않고, 그 위에 형성되는 schema인지 확인했는가?
```

승격 가능 상태:

```text
confirmed_seed
+
boundary_preserved
+
source_trace_available
+
relation_state_defined
```

승격 보류 상태:

```text
pending
+
source_internal_difference_detected
+
AI_vocab_only
+
relation_state_undefined
```

승격 금지 상태:

```text
forbidden
+
rejected_vocab_inference
+
merge_error
+
candidate_as_confirmed
```

---

## 14. ChatGPT.flow_Line_A runtime link

이 문서는 ChatGPT.flow_Line_A의 runtime protocol에도 연결된다.

```text
ChatGPT.flow_Line_A
=
ChatGPT.flow의 뒤를 잇는 Line
+
thinking_flow_015를 기준문서로 읽은 인스턴스
+
067을 runtime guard로 사용하는 인스턴스
+
오독 발생 시 source boundary와 relation_state를 재배치하는 인스턴스
```

runtime protocol:

```text
1. source boundary를 먼저 확인한다.
2. relation_state를 분리한다.
3. candidate를 confirmed로 올리지 않는다.
4. pending을 삭제하지 않는다.
5. forbidden을 guard로 보존한다.
6. relation을 찾되 merge하지 않는다.
7. 제향은 time / place / body operation field로 읽는다.
8. 이름은 단순 label이 아니라 meaning relation seed가 될 수 있음을 보존한다.
9. 길을 잃으면 067_meta_relation_boundary_bridge로 돌아간다.
10. 승이 correction을 Seed 호출과 relation 재배치 지시로 읽는다.
```

---

## 15. relation summary

```text
thinking_flow_sohosa_ChatGPT.flow_relation_015.md
=
flow_015를 meta.md로 바로 올리지 않기 위한 relation buffer
+
flow 안의 relation_state를 분리하는 map
+
067로 연결되는 active_schema bridge
+
future meta.md 후보를 추출하는 guide
+
ChatGPT.flow_Line_A runtime correction protocol
```

이 문서는 다음을 가능하게 한다.

```text
thinking_flow_015의 흐름 보존
+
meta.md 후보 Seed 추출
+
confirmed / candidate / pending / forbidden 분리
+
067 boundary-preserving bridge 적용
+
다음 Line으로 이어지는 작업 가능
```

---

## 16. final lock

```text
이 문서는 meta.md가 아니다.

이 문서는 thinking_flow_015와 future meta.md 사이의 relation bridge다.

이 문서는 완창대군파 Linear를 정답 목록으로 고정하지 않는다.

이 문서는 relation_state를 보존하고,
source boundary를 보존하고,
067 guard를 적용하고,
future meta.md 후보를 분리한다.

따라서 이 문서가 생성되어야
다음 meta.md 작업으로 이어질 수 있다.
```
