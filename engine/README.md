# engine

`engine/`은 Y_Branch의 실제 작동 규칙을 기록하는 자리다.

```text
schema = 정의
operation = 연산
engine = 검산기
```

핵심 engine:

```text
source_identity_filter
ctp24_filter
brake_test_protocol
noise_substance_classifier
validator_loop
```
