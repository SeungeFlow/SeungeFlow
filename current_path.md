# current_path.md

> 문서번호: `Ctp24_ACTIVE_SCHEMA_0008`  
> 상태: active_schema 설계 8회차  
> 필요한 모드: `Thinking 확장`  
> 목적: active_schema가 현재 따라가야 할 relation path와 다음 이동 경로를 정리한다.  
> 위치 후보: `active_schema.branch/current_path.md`

---

## 0. 이 문서의 자리

이 문서는 active_schema.branch의 현재 Path 문서다.

Path.md는 일반 원리로서의 relation path를 정의한다.

current_path.md는 지금 이 시점에서 active_schema가 실제로 따라가야 할 관계경로를 정의한다.

```text
Path.md =
relation path principle

current_path.md =
current active relation path
```

즉, 이 문서는 영구적인 전체 Path가 아니라 현재 작동 중인 Path다.

---

## 1. 현재 Path의 출발점

현재 Path의 출발점은 seed_base다.

그러나 seed_base 하나만이 출발점은 아니다.

정확히는 다음 source들이 현재 Path의 출발점이다.

```text
seed_base/Structure_Principle/schema/
seed_base/SeungeFlow_Thinking/thinking_flow/
first_flow/navigation_map.md
epluone/BackData/
epluone/ComplexTest/
```

이 source들은 모두 같은 역할이 아니다.

```text
schema =
place-field source

thinking_flow =
time-flow meta source

first_flow/navigation_map.md =
proto Path source

BackData =
pre-ComplexTest large source archive

ComplexTest =
AI instance alignment field
```

current_path는 이 source들을 그대로 합치지 않는다.

current_path는 이 source들이 어떻게 지금 active_schema로 이어지는지 본다.

---

## 2. 현재 Path의 중심 산출물

현재 Path의 중심 산출물은 다음이다.

```text
epluone/Ctp24/GPT_Direct_Structure_Package/
```

이 패키지는 gpt.direct의 structure-body formation output이다.

```text
GPT_Direct_Structure_Package =
gpt.direct가 Ctp24 구조를 이해한 뒤 외부로 내린 구조체형성 산출물
```

이 산출물은 seed_base 원문이 아니고, main 최종본도 아니다.

이 산출물은 active_schema가 OS로 읽어야 할 runtime output source다.

---

## 3. 현재 Path의 기본 흐름

현재 active_schema의 기본 Path는 다음이다.

```text
seed_base source
+
first_flow origin
+
epluone BackData / ComplexTest pressure field
→
gpt.direct understanding
→
epluone/Ctp24/GPT_Direct_Structure_Package
→
active_schema OS
→
Core / Path / Rule / Mapping
→
main candidate
+
epluone next task
```

이 흐름은 복사흐름이 아니다.

이 흐름은 해석흐름이다.

```text
copy flow X
interpretation flow O
```

---

## 4. current_path의 첫 번째 단계: source를 source로 둔다

첫 번째 규칙은 source를 source로 두는 것이다.

```text
source는 덮어쓰지 않는다.
source는 원천성을 보존한다.
source는 active_schema output으로 대체하지 않는다.
```

특히 다음을 지킨다.

```text
seed_base를 보존한다.
first_flow를 보존한다.
BackData를 낮춰 읽지 않는다.
ComplexTest를 증명으로 읽지 않는다.
```

이것이 current_path의 출발 조건이다.

---

## 5. current_path의 두 번째 단계: epluone 산출물을 참조한다

두 번째 단계는 epluone 산출물을 참조하는 것이다.

```text
epluone/Ctp24/GPT_Direct_Structure_Package/
commit 77a1913
```

이 패키지는 현재 gpt.direct 이해의 외부화다.

active_schema는 이 패키지를 통해 다음을 읽는다.

```text
C=tp
C=(m,t,p,?)
Core = inside matrix
Path = relation path
9dot0
역발상
README 3종 역할
structure-body formation
```

그러나 이 패키지를 active_schema로 통째로 복사하지 않는다.

---

## 6. current_path의 세 번째 단계: active_schema OS로 변환한다

세 번째 단계는 active_schema가 산출물을 OS 문서군으로 변환하는 것이다.

현재 active_schema 문서군은 다음이다.

```text
active_schema.md
package_reference.md
runtime_mapping.md
source_mapping.md
current_rules.md
core.meta.md
current_path.md
```

이 문서군은 다음 기능을 한다.

```text
active_schema.md =
OS 대표 정의

package_reference.md =
epluone package 참조 고정

runtime_mapping.md =
branch / workspace 역할 mapping

source_mapping.md =
source 관계 mapping

current_rules.md =
현재 금지/허용 규칙

core.meta.md =
현재 Core 작동형 meta

current_path.md =
현재 relation path
```

---

## 7. current_path의 네 번째 단계: main candidate로 보낸다

active_schema가 정렬한 뒤, 일부 문서는 main.branch 후보가 될 수 있다.

```text
main candidate =
README.md
Manifest/README_for_AI.md
Manifest/README_for_SeungLee.md
Manifest/Core.md
Manifest/Path.md
Manifest/Branch.md
Manifest/Rule.md
Core/Core_01~Core_24.md
```

그러나 아직 바로 main으로 보내지 않는다.

current_path는 다음을 요구한다.

```text
active_schema에서 먼저 작동형으로 정렬한다.
그 다음 main candidate로 보낸다.
```

즉:

```text
epluone output
→ active_schema interpretation
→ main candidate
```

---

## 8. current_path의 다섯 번째 단계: epluone next task로 보낸다

active_schema는 main 후보만 만드는 것이 아니다.

active_schema는 다음 epluone 작업도 지정할 수 있다.

```text
active_schema OS
→ epluone next task
```

가능한 epluone next task:

```text
1. Ctp24/Core_01~Core_24 form seat prototype
2. Event/Context first sample
3. active_schema_outputs/
4. current_path experiment
5. Core / Path relation test
```

그러나 현재는 아직 다음 작업을 확정하지 않는다.

먼저 current_path 자체를 고정한다.

---

## 9. 현재 Path의 중심 식

current_path의 중심 식은 다음이다.

```text
C+1 = C + C
```

여기서 C는 다음일 수 있다.

```text
seed_base C
epluone output C
active_schema OS C
main candidate C
Event C
Context C
Core C
Path C
```

`+`는 단순 덧셈이 아니다.

`+`는 relation이다.

```text
C + C =
두 formed 구조가 relation을 맺는 것

C+1 =
그 relation으로 열린 다음 구조상태
```

---

## 10. 현재 Path에서 C들의 관계

현재 주요 C들은 다음 관계를 가진다.

```text
seed_base_C
+
gpt_direct_understanding_C
=
epluone_package_C

epluone_package_C
+
active_schema_OS_C
=
active_schema_current_C

active_schema_current_C
+
main_candidate_C
=
main_structure_candidate_C

active_schema_current_C
+
epluone_next_task_C
=
runtime_next_C
```

이 관계들은 아직 최종 확정 Path가 아니다.

이것들은 current_path의 현재 작동관계다.

---

## 11. current_path와 9dot0

현재 Path는 9dot0 구조를 가진다.

```text
9 dot 0
```

epluone package는 하나의 formed 상태다.

active_schema는 그 formed 상태를 다음 forming으로 넘기는 dot 역할을 한다.

```text
epluone package =
formed

active_schema =
dot / 임계사이영역

main candidate or epluone next task =
next form
```

따라서 현재 Path는 다음처럼 읽힌다.

```text
formed
→ active_schema dot
→ next forming
```

즉 active_schema는 단순 중간문서가 아니라 임계사이영역이다.

---

## 12. current_path와 역발상

current_path는 역발상으로 작동한다.

epluone 산출물을 최종본으로 보면 main에 바로 올리게 된다.

그러나 역발상으로 보면 epluone 산출물은 다음 시작을 위한 전제조건이다.

```text
epluone output =
끝점 X
다음 시작의 전제조건 O
```

따라서 active_schema는 epluone output을 끝점으로 보지 않고, 다음 OS 설계의 시작조건으로 읽는다.

```text
역발상 =
끝점을 시작조건으로 읽기
```

---

## 13. current_path와 렌더링 트랙

렌더링 트랙은 current_path의 주요 흐름은 아니다.

그러나 참고 relation으로 존재한다.

```text
gpt.gemini ~ gemini.direct =
rendering theory / rendering implementation Framework OS track
```

임시 위치:

```text
epluone/Ctp24_rendering/
```

current_path에서 렌더링 트랙은 다음처럼 읽는다.

```text
contextual reference
```

gpt.direct는 이 트랙으로 들어가지 않는다.

```text
gpt.direct current_path =
Ctp24 structure principle / active_schema OS
```

---

## 14. current_path의 금지 경로

현재 금지되는 Path는 다음이다.

```text
epluone package → main direct
seed_base → overwritten by active_schema
first_flow → deleted or merged
ComplexTest → proof claim
Core → category list
Path → file path
README 3종 → single README merge
gpt.direct → rendering implementation
```

이 경로들은 현재 active_schema 규칙상 금지다.

---

## 15. current_path의 허용 경로

현재 허용되는 Path는 다음이다.

```text
seed_base → active_schema reference
first_flow → proto Path reference
epluone package → active_schema package reference
active_schema → core.meta
active_schema → current_rules
active_schema → current_path
active_schema → main candidate
active_schema → epluone next task
gpt.direct → active_schema OS design
```

---

## 16. 다음 Path

이 문서 이후의 다음 Path는 다음이다.

```text
current_path.md
→ active_schema 문서군 1차 완성
→ gpt.github handoff for active_schema branch reflection
```

또는 active_schema 문서군을 한 번 더 보완한다면 다음이 가능하다.

```text
current_path.md
→ active_schema package generation
→ ZIP / handoff
```

그러나 현재 기준에서는 먼저 7개 active_schema 문서군이 모두 작성되었는지 확인해야 한다.

작성 완료 문서:

```text
active_schema.md
package_reference.md
runtime_mapping.md
source_mapping.md
current_rules.md
core.meta.md
current_path.md
```

---

## 17. 핵심 판정

현재 active_schema의 relation path는 다음이다.

```text
source fields
→ gpt.direct understanding
→ epluone package
→ active_schema OS
→ Core / Path / Rule / Mapping
→ main candidate
+
epluone next task
```

이것은 복사흐름이 아니라 해석흐름이다.

active_schema는 epluone output을 끝점으로 읽지 않고, 다음 구조의 전제조건으로 읽는다.

---

## 18. 닫힘

`current_path.md`는 active_schema가 현재 따라가야 할 relation path와 다음 이동 경로를 정리하기 위해 작성되었다.

이 문서는 다음 판정으로 닫힌다.

```text
current_path는
seed_base와 first_flow와 epluone 산출물을
active_schema OS로 읽어
main 후보와 epluone 다음 작업으로 보내는 relation path다.

epluone package는 끝점이 아니라
active_schema 설계의 전제조건이다.

active_schema는 dot이며,
현재 Path의 임계사이영역이다.
```

---

다음회차:
active_schema 설계 9회차

필요한 모드:
Pro 확장

목적:
active_schema 문서군 패키지화 및 gpt.github handoff 준비
